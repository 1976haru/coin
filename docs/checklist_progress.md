# Checklist Progress — 96항목 진척도

마지막 업데이트: 2026-05-18 (#30 Trend Following 확장 — `backend/app/strategies/trend_following_contract.py` 신규 `TrendFollowingContractStrategy(StrategyContract)` (#29 ABC 구현). 파라미터: `TrendFollowingParams`(ema_fast=20/ema_slow=60/trend_sma_period=200/donchian_period=20/adx_period=14/adx_min=18.0/breakout_buffer_pct/min_candles_required/allow_short_candidates=False/base_notional_usdt=100/max_leverage_hint=1.0). `capability.name="trend_following_v2"` + `enabled_by_default=False` + `preferred_regimes=("TREND_UP", "TREND_DOWN")` — StrategySelectionAgent 가 trending regime 활성화. `_indicators.py` 에 `true_range`/`donchian_channel(exclude_current=True)`/`adx`(Wilder 평활) 추가 — 모두 결정론, 데이터 부족 시 안전 기본값. 4 abstract 구현: `generate_signal`(quality=EXCLUDE→BLOCKED, freshness stale→BLOCKED, universe 외→BLOCKED, high-risk notice→BLOCKED, 캔들 부족→NO_ACTION, OHLC 길이 불일치→NO_ACTION, ADX<adx_min→HOLD, close>SMA200+EMA정배열+Donchian breakout+ADX≥min→BUY candidate, allow_short_candidates=True 시 미러 SHORT candidate. confidence=0.5+min(0.4, (adx-adx_min)/100). stop=close±1.5×ATR, tp=close±3×ATR), `calculate_size`(BUY/SELL 시 base_notional×confidence, HOLD/BLOCKED/NO_ACTION 시 0. `is_final_order_size=False` 영구), `exit_rule`(quality EXCLUDE→fraction=1 critical, freshness stale→fraction=1 high, high-risk notice→fraction=1 high, fast<slow EMA→fraction=0.6 normal, close<Donchian low→fraction=1 high, ADX<adx_min×0.7→fraction=0.3 normal, 정상 추세→should_exit=False. `is_order_intent=False` 영구), `explain_signal`(summary 에 "candidate only — not an order" 명시, evidence 에 EMA/SMA/ADX/Donchian/data_quality/freshness/universe/regime, risks 에 stale/EXCLUDE/notice/theme review, limitations 에 RiskManager 최종 결정 명시). 회귀 45 케이스 (`tests/test_trend_following_contract.py`) — Indicators 10(SMA·EMA·ATR·true_range·Donchian·ADX) / Signal generation 10(강한 uptrend BUY · 약한 추세 HOLD · quality·freshness·universe·notice·candles 부족·OHLC 불일치 BLOCKED/NO_ACTION · short 옵트인 토글) / Sizing 3(zero·proportional·is_final_order_size=False) / Exit rule 5(quality·freshness·notice·EMA cross·정상) / Explanation 4(candidate only·evidence·risks·limitations) / evaluate() 2 / Static guards 4(broker·execution·SDK·order method·forbidden literal 부재) / Registry+SelectionAgent 5(register·create·preferred_regimes·TREND_UP 활성·RANGE 제외). 기존 `tests/test_trend_following.py`(#30 1차, 14 케이스) 회귀 없음 — 기존 `TrendFollowingStrategy` Protocol 기반 그대로 유지. `docs/trend_following.md` 신규 + `docs/strategy_base.md` 에 cross-link. backend 2178 passed / 1 skipped (이전 2133 → 2178, +45). 본 단계 완료는 실거래 허가 아님 — 31번 이후 개별 전략 구현은 본 작업 범위 아님.

이전 업데이트: 2026-05-18 (#29 StrategyBase / StrategyContract 확장 — `backend/app/strategies/contract.py` 신규 `StrategyContract` ABC (4개 abstract method: `generate_signal`/`calculate_size`/`exit_rule`/`explain_signal`) + `StrategyContext` (frozen, symbol/timeframe/closes/highs/lows/volumes/freshness_ok/data_quality_grade/notice_context/theme_context/regime/positions_snapshot/extra — `_assert_no_secret_keys` 로 secret 차단) + `PositionSizingHint` (suggested_qty/suggested_notional_usdt/leverage_hint, `is_final_order_size=False` 영구) + `ExitRuleDecision` (should_exit/exit_qty_fraction 0..1/urgency normal·high·critical, `is_order_intent=False` 영구) + `SignalExplanation` (strategy_name/symbol/summary/reasons/evidence/risks/limitations/confidence/generated_at) + `evaluate()` 편의 메서드 (is_order_intent=True/is_final_order_size=True 반환 시 `StrategyContractError` raise) + `ALLOWED_SIGNAL_ACTIONS` 6종(BUY/SELL/HOLD/BLOCKED/NO_ACTION/WATCH_ONLY) + `assert_no_order_intent`/`is_safe_action` 헬퍼. `backend/app/strategies/contract_registry.py` 신규 `ContractRegistry` (register_strategy `StrategyContract` 하위만/중복 name 거부/get/list/catalog/create_strategy/filter_by_market_regime(prefs+UNKNOWN inclusive)/filter_by_symbol(pair 제외)/filter_enabled/set_enabled, `enabled_by_default=False` 기본). `backend/app/agents/strategy_selection.py` 신규 hook (`StrategyActivationContext`/`StrategyActivationDecision` `direct_order_allowed=False` 영구 + `select_active_strategies`) — 본격 구현은 후속, 본 단계는 hook 만. 회귀 42 케이스 (`tests/test_strategy_contract.py`) — ABC instantiation / evaluate raise on is_order_intent=True / StrategyContext secret 키 거부 + list→tuple 변환 / PositionSizingHint broker 필드 부재 / ExitRuleDecision 범위 검증 / SignalExplanation / ContractRegistry 8개 / SelectionAgent hook 6개 / safety helpers / **정적 회귀** (app/strategies/ 에 app.brokers·execution·order_gateway import 부재 + .place_order/.cancel_order/.get_balance/.submit_order 호출 부재 + contract*.py 에 requests/httpx/ccxt/pyupbit/binance/okx SDK import 부재 + ENABLE_LIVE_TRADING=True / is_order_intent: bool = True literal 부재 + StrategySelectionAgent brokers 미참조) / Freshness/Quality 통합 sample / notice·theme context 가 explanation 에 포함되어도 주문 신호로 변환 안 됨. `docs/strategy_base.md` 신규. 기존 `tests/test_strategy_base.py` 35 케이스 (#29 1차 Protocol 기반) 회귀 없음 — 4개 기존 전략 그대로 유지. backend 2133 passed / 1 skipped (이전 2087 → 2133, +46). 본 단계 완료는 실거래 허가 아님 — 30번 이후 개별 전략 구현은 본 작업 범위가 아니다.

이전 업데이트: 2026-05-18 (#28 Env Profiles + Startup Guard — `backend/app/core/profile.py` 신규 `AppProfile` 4단계 enum (PAPER/SHADOW/LIVE/TEST) + `KeyProfile` 4단계 + `SecretClassification` 4단계 (SAFE/PLACEHOLDER/TEST_LOOKING/REAL_LOOKING) + `classify_secret_value`/`looks_like_real_secret`/`is_safe_secret` (Shannon entropy 기반 분류) + `mask_secret` 재export (#27) + `GuardViolation` + `StartupGuardResult` + `validate_startup_profile(env)` 순수 함수 + `enforce_startup_profile(strict)` strict mode + `StartupGuardError`. 8개 검증 도메인 — withdrawal 영구 금지 / PAPER (LIVE 키 / ENABLE_LIVE_TRADING / TRADING_MODE=LIVE_*) / SHADOW (trade key / live flag) / LIVE (confirmation phrase / enable flag / KEY_PROFILE / sandbox-only key / sandbox-only flag / public env secret) / TEST (real-looking key / live flag) / TRADING_MODE 정합성 / public VITE_·NEXT_PUBLIC_*SECRET. 모든 secret 변수 mask_secret 으로 정리된 `masked_env_summary` 노출. `backend/app/api/profile.py` 신규 `GET /api/profile` (read-only snapshot + masked summary + warning). `.env.example` 신규 변수 — APP_PROFILE=PAPER / KEY_PROFILE=PAPER / LIVE_CONFIRMATION= (빈 값) / REQUIRE_LOCAL_SECRETS=true / ALLOW_SANDBOX_KEYS_ONLY=false / STARTUP_GUARD_STRICT=false. `.gitignore` 확장 — .env.live / .env.shadow / .env.paper / .env.paper.local / .env.test.local 패턴 + backend/frontend 하위 동등 패턴. 회귀 53 케이스 (`tests/test_env_profiles.py`) — Profile enums 4 / Secret classification 5 / PAPER 5 / SHADOW 2 / LIVE 6 / TEST 3 / withdrawal parametrized 4 / public env 2 / strict 4 / masked summary 2 / REST API 1 / .env.example/.gitignore/docs 5 / 모듈 정적 회귀 (network import / forbidden literal / Strategy·Agent 직접 import 부재) 4. `docs/env_profiles.md` 신규 + api_key_policy.md / sandbox_paper_keys.md 에 cross-link. backend 2087 passed / 1 skipped (이전 2034 → 2087, +53). 기존 `test_sandbox_paper_keys.py` #28 1차 + `test_api_key_policy.py` #27 회귀 없음. 본 단계 완료는 실거래 허가 아님 — strict 모드는 배포 스크립트 전용. 29번 이후 미수정.)

이전 업데이트: 2026-05-18 (#27 Secret Permissions 확장 — `docs/api_key_policy.md` 에 §8 거래소별 권한 상세 정책(Upbit 조회/주문/출금 매핑, OKX API key+secret+passphrase + IP allowlist + 50011 link, Binance Global vs Binance.US 구분 + Enable Reading/Spot Trading/Futures/Withdrawals/Internal Transfer 명시) + §9 권한 체크리스트 통합 표 (MUST_OFF/DEFAULT_OFF/READ_LIMITED/TRADE_GATED 4분류, 15개 권한) + §10 스크린샷 보관 가이드 (마스킹 의무 7종, 파일명 규칙 `api-permission-<exchange>-<tier>-<YYYYMMDD>-redacted.png`, repository 밖 보관) + §11 `.env.example` 작성 원칙 (실제 키 금지, frontend VITE_/NEXT_PUBLIC_ secret 금지) + §12 운영 전 확인 절차 (9단계 체크리스트) + §13 위반 시 대응 (8단계 incident response) + §14 27번 범위 명시 (실거래 허가 아님). `backend/app/audit/secret_masking.py` 신규 — `mask_secret(value, prefix_chars, suffix_chars)` (None→`<unset>`, placeholder→`<placeholder>`, 짧은 값→`***`, 긴 값→`prefix***suffix`) + `mask_dict_values` 헬퍼. `scripts/check_secret_policy.py` 신규 — repository 정적 스캔 (long secret var assignment, inline secret literal, frontend VITE_/NEXT_PUBLIC_ secret, withdraw endpoint URL, withdraw 메서드 정의, dangerous flag=true). 테스트 파일 제외(부정-예시 fixture 보유). exit code 0/1. `backend/tests/test_secret_permissions_policy.py` 신규 30 케이스 — mask_secret 동작 7개 + 정책 문서 추가 섹션 phrase 11개 + .env.example 실제 키 부재 + frontend secret literal 부재 (UPBIT/OKX/Binance/generic + VITE_/NEXT_PUBLIC_ secret) + production 코드에 withdraw 메서드·endpoint URL 부재 + ORM 컬럼 secret 부재 + LIVE flag 기본 false + ENABLE_WITHDRAWAL monkeypatch=true 도 false 유지 + AdapterCapability requires_secret 메타만 + logs 디렉터리 tracked secret 부재 + checklist 27 ✅ 표기 + scripts/check_secret_policy.py 존재. backend 2034 passed / 1 skipped (이전 1995 → 2025, +30, 기존 test_api_key_policy.py 33 케이스 회귀 없음). 본 단계 완료는 실거래 허가 아님 — 정책 문서 + 회귀 테스트 작성만.

이전 업데이트: 2026-05-18 (#26 API Rate Limit Guard — `app/brokers/api_limits.py` 신규 `RateLimitPolicy`(exchange/group/capacity/refill_rate_per_sec/safety_buffer/unit=req|weight/cooldown_on_429/418/okx_50011/network/max_retries/base_backoff/max_backoff/disabled) + 거래소별 preset 10개(upbit quotation/exchange disabled, okx public/private disabled/trade disabled, binance spot_public weight 1200/min·spot_private disabled·futures disabled, mock·paper) + parse_retry_after + parse_okx_error + parse_upbit_remaining_req + parse_binance_used_weight 통합 wrapper. `app/brokers/rate_limit_guard.py` 신규 `RateLimitGuard` (`can_call`/`acquire`/`update_from_response`/`update_from_error`/`reset_retry`/`snapshot`/`reset`) + `GuardState` (remaining/used_weight/cooldown_until/consecutive_failures/last_error + stats: total_calls/acquired/throttled/429/418/okx_50011/network/retries_issued/denied) + `AcquireDecision` + `RetryDecision` + `ExchangeRateLimitRegistry` + `build_default_registry(preload=True)`. `ERROR_KIND_*` 카탈로그 8개. 무한 재시도 방지 — max_retries 초과 시 should_retry=False, exponential backoff(base*2^attempt, max_backoff cap), Retry-After 헤더 우선, recursive retry 부재, while True 부재. `app/api/rate_limits.py` 신규 — `GET /api/rate-limits` (read-only snapshot, secret 미노출). 회귀 53 케이스 (`tests/test_rate_limit_guard.py`) — Policy validation / default policies / parse_retry_after·okx_error·binance_used_weight·upbit_remaining_req / can_call·acquire / disabled / safety_buffer / cooldown / response 헤더 자동 반영 (Upbit Remaining-Req·Binance X-MBX-USED-WEIGHT·OKX 50011 body) / update_from_error 429·418·50011·network·auth·invalid 별칭 / max_retries 초과 / 무한 루프 방지 / exponential backoff + cap / Retry-After 우선 / reset / snapshot secret 부재 / Registry get·register·snapshot_all·preload / REST API / 금지 문자열 (while True/ENABLE_LIVE_TRADING/requests·httpx·ccxt·pyupbit·binance·okx import) 부재 / Strategy·Agent 직접 import 부재. `docs/api_limits.md` 신규. backend 1995 passed / 1 skipped, frontend 변경 없음. 27번 이후 미수정.)

이전 업데이트: 2026-05-18 (#25 Paper Broker — `app/brokers/paper_market_broker.py` 신규 `PaperMarketBroker` (read-only 시세 source + 가상 체결, mode=PAPER 영구) + `PaperMarketBrokerConfig` (fee_bps/slippage_bps/allow_short/allow_margin/max_order_notional/require_source/universe whitelist/max_ticker_age_sec/initial_balances) + `PaperMarketSource` Protocol (`MockExchangeAdapter`/Upbit/OKX/Binance read-only adapter 모두 호환) + `make_paper_universe` 헬퍼. `app/brokers/paper_trader.py` 신규 `PaperTrader` 컨트롤러 (source 선택 mock/upbit_readonly/okx_readonly/binance_readonly/kis_readonly_stub, start/stop/reset, OrderGateway 경유 `submit_paper_order_via_gateway`, KIS 는 disabled stub + warning) + `PaperStatus` + `PaperOrderLogEntry`. `app/api/paper.py` 신규 — GET `/api/paper/{status,orders,sources}` + POST `/api/paper/{start,stop,reset,source}` (admin). 모든 응답에 `mode=PAPER`, `is_real_trade=False`, `execution_source` (paper_broker/paper_trader), `warning="Paper execution only. Not real profit or real trade."`, `fill_quality_warning="Paper fills may differ from live execution..."` 포함. universe 밖 BUY → review_required REJECTED (EXIT 는 허용), stale ticker BUY 차단 (EXIT 허용), LIVE mode (`mode`/`trading_mode`) 거부, idempotent client_order_id, audit secret sanitize. `tests/test_paper_broker.py` 신규 46 케이스 — source 없음 거부 / MARKET BUY+SELL 잔고·포지션 / LIMIT crossable·open·cancel / universe whitelist / staleness / fee+slippage / allow_short·allow_margin / dup client_order_id / LIVE 거부 / envelope 필드 / audit secret / account_summary / PaperTrader source 선택 + KIS warning / start·stop·reset / submit_via_gateway 위임·로그·envelope / running=false 거부 / LIVE 사전 차단 / gateway 없으면 거부 / 로그 필터 / REST 7개 endpoint admin gating / 정적 회귀 (paper 모듈에 requests·httpx·ccxt·pyupbit·binance·okx import 부재, strategies·agents 가 paper 모듈 직접 import/instantiate 부재) / MockExchangeAdapter 호환. `docs/paper_broker.md` 신규. backend 1942 passed / 1 skipped, frontend 변경 없음. 26번 이후 미수정.)

이전 업데이트: 2026-05-18 (#24 Mock Broker (simulation) — `app/brokers/mock_simulation.py` 신규: `MockBroker` (top-level facade) + `MockBrokerConfig` (frozen — fee_bps/slippage_bps/allow_short/allow_margin/partial_fill_enabled/max_order_notional/mode/initial_balances, LIVE mode 생성 시 ValueError) + `MockAccountState` (다중 자산 free/locked, lock/unlock/settle_buy/settle_sell, allow_negative_base 옵션) + `MockPositionBook` (avg_entry_price 가중평균, realized/unrealized PnL, 롱→숏 전환) + `MockMarket` (deterministic hash 가격 fallback + set/get) + `MockExecutionEngine` (fee/slippage 결정론 — random 사용 없음). place_order/cancel_order/get_order/get_balance/get_position/get_account_summary/set_market_price/reset 8개 공개 메서드. MARKET 즉시 FILLED + LIMIT crossable/non-crossable 자동 분기 + open LIMIT 의 locked balance + cancel 시 자동 unlock + duplicate client_order_id idempotent + LIVE mode 거부(mode/trading_mode) + allow_short=false 보유 초과 SELL 거부 + allow_margin=false quote 잔고 초과 BUY 거부 + max_order_notional 한도 + audit secret sanitize. 모든 응답에 `mode`(MOCK/PAPER), `is_real_trade=False`, `execution_source="mock_broker"`, `warning="Mock execution only. Not real profit or real trade."` 포함. `PaperBroker.place_order(dict)→dict` 시그니처 호환 — OrderGateway drop-in 가능. `tests/test_mock_broker.py` 신규 51 케이스 — 초기 잔고 / MARKET BUY+SELL FILLED + 가중평균 / LIMIT crossable·non-crossable·open·cancel + locked 해제 / fee+slippage / unrealized·realized PnL / max_order_notional / allow_short·allow_margin / dup client_order_id / LIVE 거부 / invalid input / audit secret 부재 / reset / account summary / MockAccountState·MockPositionBook 단위 / mock_simulation.py 내 requests·httpx·ccxt·pyupbit·binance·okx import 부재 + ENABLE_LIVE_TRADING=True literal 부재 / strategies·agents 직접 호출 부재 / PaperBroker 시그니처 호환. `docs/mock_broker.md` 신규. backend 1896 passed / 1 skipped, frontend 변경 없음. 25번 이후 미수정.)

상태: ☐ 미착수 · ◔ 부분 · ◑ 절반 · ◕ 거의 완료 · ✅ PASS

## Phase 0 — 방향/범위 (5)
| # | 항목 | 상태 | 산출물 |
|---:|---|:---:|---|
| 1 | Product Scope | ✅ | `docs/product_scope.md` (본문 + MOCA 허브 + MVP/제외/승격 + 회귀 테스트) |
| 2 | Strategy Portfolio | ✅ | `docs/strategy_portfolio.md` (4대 전략 카드 + 장세 매트릭스 + 김프 특수 정책 + 회귀 테스트) |
| 3 | Operating Modes | ✅ | `app/core/modes.py` (TradingMode + ModeCapability + allowed_transitions) + `docs/operating_modes.md` (행동 매트릭스 + 전환 그래프) + 회귀 테스트 |
| 4 | Safety Principles | ✅ | `docs/safety_principles.md` (12 섹션, 단일 경로 + 모듈 경계 + 강제 메커니즘 표) + `CLAUDE.md` + 회귀 테스트 43개 |
| 5 | Agent Trader Naming | ✅ | `app/core/app_info.py` (+ ReleaseNote), `app/api/info.py` (/api/app, /api/release-notes), `frontend/index.html` 브랜드 갱신 + 릴리즈 노트 모달, `frontend/src/appInfo.ts` placeholder + 회귀 테스트 21개 |

## Phase 1 — 시스템 아키텍처 (7)
| # | 항목 | 상태 | 산출물 |
|---:|---|:---:|---|
| 6 | Backend Skeleton | ✅ | `backend/app/{api,core,db,market,brokers,strategies,risk,execution,agents,governance,audit,schemas}` 구조 완료 |
| 7 | Frontend Skeleton | ✅ | Vite 5 + React 18 + TypeScript 5.5 + react-router 6. `frontend/{index.html,package.json,tsconfig.json,vite.config.ts,src/{main.tsx,App.tsx,api/{client,health,watchlist,audit}.ts,components/{Header,StatusCard}.tsx,pages/{Dashboard,Watchlist,Audit}Page.tsx,styles/global.css}}`. 빌드 검증 완료 (44 modules, 170 KB JS). 레거시 데모는 `legacy_demo.html` 로 보존. |
| 8 | Shared Schemas | ✅ | `app/schemas/{market,signal,order,position,risk,agent}.py` + `__init__.py` 단일 진입점. `is_order_intent: bool = False` 강제 (StrategySignal/PairSignal/KimpSignal/AgentDecision). `OrderRequest`/`OrderResult`/`Position`/`AccountSnapshot` 신규. 회귀 테스트 18개 (`test_schemas.py`) |
| 9 | Config Layer | ✅ | `app/core/config.py` + `Settings.summary()` (redaction) + `Settings.validate()` (운영 경고) + `ENV_VARS_REFERENCED` 카탈로그. REST: `GET /api/config/warnings` (공개), `GET /api/config/effective` (admin). `/api/status.safety_warnings` 추가. `.env.example` 파리티 회귀 테스트. `docs/config_layer.md` 설계 문서. YAML 미도입 (의도적). |
| 10 | Feature Flags | ✅ | `app/core/feature_flags.py` + 회귀 테스트 |
| 11 | Audit Foundation | ✅ | `app/audit/{audit_log,redaction,order_audit,agent_decision_log}.py`. AuditLog.record()가 redaction 자동 적용. OrderAuditLog는 lifecycle 매핑/검색, AgentDecisionLog는 is_order_intent 강제 기록. 회귀 테스트 18개 (`test_audit_foundation.py`) |
| 12 | CLAUDE.md | ✅ | 루트 CLAUDE.md |

## Phase 2 — 데이터/DB (7)
| # | 항목 | 상태 |
|---:|---|:---:|
| 13 | Database Schema | ✅ | `app/db/{models,session,migrations/}` + `alembic.ini`. 4개 공통 테이블(audit_events/orders/agent_decisions/positions). is_order_intent 기본 false 컬럼 강제. SQLAlchemy 2.0 + Alembic. **2026-05-17 추가**: 코인 전용 9개 테이블 (coin_symbol/coin_candle/coin_tick/coin_orderbook_snapshot/coin_signal/coin_order/coin_trade/coin_position/coin_risk_event) + `0003_crypto_schema.py` 마이그레이션 + `docs/crypto_database_schema.md`. 가격/수량 Numeric(28,12). CoinSignal.used_for_order 기본 False (advisory). CoinOrder.mode 기본 "PAPER" (LIVE 아님). secret 컬럼 부재 회귀. |
| 14 | Watchlist/Universe | ✅ | `WatchlistEntry` 모델 (list_name/symbol/exchange UNIQUE) + `app/market/watchlist.py` 서비스 (CRUD + list_names + remove_by_list) + `0002_watchlist.py` 마이그레이션 + REST API (`GET /api/watchlist`, `POST/DELETE/PATCH` admin 토큰). **2026-05-17 보강**: 정규화(symbol upper / exchange·list_name lower / strip) + 검증(빈/공백/길이/허용 거래소 화이트리스트) + universe 크기 cap (list_name별 default=50/majors=20/kimp_pairs=100 + 전체 `WATCHLIST_MAX_ENABLED_TOTAL` env, 기본 100) + `summary()` API 응답(by_exchange/by_list_name/limits) + seed 템플릿 `config/watchlists/{default,majors,kimp_pairs}.json` + 멱등 CLI `app.market.watchlist_seed`. 정책: `docs/watchlist_universe.md` (Watchlist 는 주문 허용 목록이 아니라 후보 universe 제한 장치 — RiskManager/OrderGuard/PermissionGate 통과 필수). |
| 15 | Market Data Collector | ✅ | `app/market/collector.py` (`MarketDataSource` Protocol + `MockMarketDataSource` 결정론 + `MarketDataCollector` ticker 캐시 + freshness 통합 + 예외 시 캐시 보존). REST: `GET /api/market/tickers`, `POST /api/market/collect` (admin, watchlist 기반). 거래소 SDK 직접 의존 없음 — 실제 source는 #21·#22에서 추가. **2026-05-17 확장**: OHLCV/FundingRate/FxRate 신규 데이터 모델, `MockMarketDataSource` 가 `fetch_ohlcv/fetch_funding/fetch_fx` 지원(spot 은 funding=None), `collect_all(includes={ticker,ohlcv,orderbook,funding,fx}, max_symbols, list_name, ...)` 다중 데이터 수집, `EmptyWatchlistError` 로 전체 시장 fallback 차단, dedup + `MARKET_COLLECTOR_MAX_SYMBOLS` env (기본 100), 부분 실패 격리 + summary. `market_persister.persist_report` 가 13번 `coin_candle/coin_tick/coin_orderbook_snapshot` 로 영속화 (UNIQUE 충돌 skip). REST: `POST /api/market/collect` body 확장(include/timeframe/limit/orderbook_depth/fx_pairs/persist), `GET /api/market/collector/status`. Frontend `/market` 페이지 read-only. 문서: `docs/market_data_collector.md`. 회귀: Protocol 에 주문 메서드 부재 + collector 가 broker/execution/ccxt/pyupbit import 금지 + 금지 문자열 부재. |
| 16 | Data Freshness | ✅ | `app/market/freshness.py` — 순수 함수(check_timestamp_freshness/check_feed_freshness/should_block_new_buy) 유지 + `FreshnessPolicy`(data_type 별 max_age + block_buy_when_stale/reconnecting) + `FreshnessTracker`(mark_seen/mark_reconnecting/clear/evaluate/can_open_new_position/evaluate_for_order/get_summary) + side 분류(ENTRY vs EXIT, SELL/EXIT/CLOSE 는 freshness 차단 면제). is_stale: None=stale, future=stale, max_age<=0=stale. Collector(#15) 가 ticker/ohlcv/orderbook/funding/fx 수집 성공 시 tracker.mark_seen 자동 호출 (실패는 갱신 안 함). `/api/order/preview` 가 tracker.evaluate_for_order 로 freshness_statuses 생성. REST: GET `/api/freshness`(summary 포함, legacy ok/reason 키 유지), POST/DELETE `/api/freshness/reconnecting`(admin). env: `MARKET_FRESHNESS_{TICKER,ORDERBOOK,OHLCV,FUNDING,FX}_MAX_AGE_SECONDS`, `MARKET_BLOCK_BUY_WHEN_STALE`, `MARKET_BLOCK_BUY_WHEN_RECONNECTING`. Frontend `/market` 에 Freshness 패널(counts/blocks_new_entries/reconnecting/stale 목록). 문서: `docs/data_freshness.md`. 실제 WebSocket 구현은 후속 단계. |
| 17 | Data Quality | ✅ | `app/market/quality.py` (spread/volume/orderbook depth/top-size/fx anomaly/spike + `QualityReport.liquidity_ok`/`fx_anomaly_ok` Strategy 호환 플래그). CLI: `scripts/check_data_quality.py` (exit code 0/1/2). **2026-05-17 추가**: 별도 historical candle 계층 `app/market/data_quality.py` — DataQualityGrade(GOOD/WARNING/EXCLUDE) + 6 검사(missing/duplicate/OHLC/volume/price-outlier/off-universe) + run_day_check + `BacktestPromotionGuard.evaluate` + `load_candles_for_day`. CLI `--symbol` 으로 historical 모드 진입(--date/--from-date/--to-date/--output json/--fail-on-exclude, exit 0/2). REST `GET /api/market/data-quality/summary?symbol=&exchange=&timeframe=&date=` (public, secret 미노출). Watchlist universe 강제(`--symbol` 필수). 문서: `docs/data_quality.md` — Data Freshness(#16) 와의 역할 분리 명시. |
| 18 | Exchange Notices | ✅ | (1차) `app/market/notices.py` (Notice/NoticeRegistry/assess_symbol_notices/block_reasons + 6개 NoticeKind + 시간창 active 필터). REST: `GET/POST/DELETE /api/notices` + `GET /api/notices/symbol/{exchange}/{symbol}`. KimpStrategy의 `deposit_withdrawal_ok` 직결. **2026-05-18 확장**: DB-backed 영속 계층 추가 — `ExchangeNotice` ORM 모델 + `0004_exchange_notices.py` 마이그레이션((exchange,notice_id) + (exchange,content_hash) UNIQUE). `app/market/notice_collector.py`: `NoticeSource` Protocol + `MockNoticeSource`(외부 호출 0, 결정론 8개 fixture) + `NoticeCollector` (정규화/분류/dedup/upsert). 8개 notice_type (DEPOSIT_WITHDRAWAL_SUSPENSION/CAUTION/DELISTING/LISTING/MAINTENANCE/TRADING_SUSPENSION/POLICY/OTHER) + 4단계 severity (INFO/WARNING/HIGH/CRITICAL) + 한·영 키워드 기반 분류 + symbol whitelist 기반 추출(BTC/ETH/XRP 등). `app/market/notice_context.py`: `NoticeContextBuilder` (build_notice_context/get_symbol_risk_flags + `direct_order_allowed=False` 영구). REST 확장: `POST /api/notices/collect`(admin, 기본 mock source), `GET /api/notices/context`(symbols/lookback_hours), `GET /api/notices/types`(카탈로그), `GET /api/notices` 응답에 `exchange_notices/summary` 추가. `ThemeInsightAgent.decide()` 가 ctx["notice_context"] 가 있으면 explain_text 에 부가 — 의사결정은 변경 없음. 문서: `docs/exchange_notices.md`. 회귀: notice_collector/notice_context 가 `app.brokers.*` / `app.execution.*` 미참조 + 금지 문자열 부재 (test_notice_modules_dont_*). 공지 이벤트는 후보 필터/리스크 설명 전용 — 직접 주문 트리거 아님 (CLAUDE.md §2.3). |
| 19 | Trend/News/Theme Signals | ✅ | (1차) `app/market/themes.py` (`classify_regime` 순수 함수 + `ThemeRegistry` (와일드카드 거래소 + 다중 태그) + `NewsRegistry` (시간창 + 시장 전반/심볼별) + `assess_market_context` Agent dict). REST: 7개 엔드포인트. AgentOrchestrator의 context["regime"] 직결. **2026-05-18 확장**: DB-backed 영속 계층 — `ThemeSignal` ORM 모델 + `0005_theme_signals.py` 마이그레이션((source,provider,signal_id) + (source,provider,content_hash) UNIQUE). `app/market/theme_signals.py`: `ThemeProvider` Protocol + `MockThemeProvider`(외부 호출 0, 결정론 9개 fixture, trend/news/disclosure/theme/macro_fx 다중 source 커버) + `ThemeSignalCollector` (정규화/분류/dedup/upsert). 8개 risk_flag 화이트리스트 (high_news_attention/regulatory_attention/exchange_risk_attention/delisting_related_theme/suspicious_hype_theme/macro_fx_attention/review_required/context_only) — BUY/SELL/ENTER/EXIT 토큰은 본 계층 어디에도 등장하지 않음 (정적 + 동적 회귀로 강제). `used_for_order` + `direct_order_allowed` 영구 False (DB 컬럼 / API 응답 / Agent context 모두). `app/market/theme_context.py`: `ThemeFilter` + `ThemeContextBuilder` (read-only — candidate_filter_ok / candidate_filter_review_required 만 사용, action 컬럼 부재 + `_assert_no_action_tokens` 내장 가드). REST 확장: `GET /api/theme-signals`, `POST /api/theme-signals/collect`(admin, mock provider 기본), `GET /api/theme-signals/context`(symbols/themes_csv/sources_csv/lookback_hours), `GET /api/theme-signals/sources`(카탈로그), `POST /api/theme-signals/filter`(Watchlist 후보 → review flag). `ThemeInsightAgent.decide()` 가 ctx["theme_context"] 가 있으면 explain_text 에 부가 — 의사결정은 변경 없음. 문서: `docs/theme_signals.md`. 회귀: theme_signals/theme_context 가 `app.brokers.*` / `app.execution.*` 미참조 + 금지 문자열/액션 토큰 부재 (test_theme_signal_modules_no_*). Theme/News/Trend 데이터는 후보 필터/리스크 설명 전용 — 직접 매매 신호가 아님 (CLAUDE.md §2.3). |

## Phase 3 — 브로커/API (9)
| # | 항목 | 상태 |
|---:|---|:---:|
| 20 | Exchange Adapter Interface | ✅ | (1차) `app/brokers/base.py` 풀 구현: `ExchangeAdapter` ABC + `AdapterCapability` (READ_ONLY/PAPER/SANDBOX/LIVE) + capability 외 동작은 `ExchangeAdapterDisabledError`. MarketDataSource Protocol 호환 (collector 직접 주입). 출금 메서드 부재 회귀(`assert_no_withdrawal_methods`). **2026-05-18 확장**: 스펙 메서드 alias `fetch_price`(→ fetch_ticker.price), `get_balance`(→ fetch_balance) 추가. base `place_order` 가 `capability.mode == "LIVE"` + `settings.enable_live_trading == False` 조합을 사전 거부 (`status="REJECTED"`, `route="live_not_wired"`) — 구현체가 우회 못함. `client_order_id` 또는 `idempotency_key` 가 dict 에 없으면 base 가 패스스루 정규화. `MockExchangeAdapter` 보강: client_order_id 기반 idempotent (중복 호출 동일 결과 + 잔고 이중 차감 없음), 잔고 부족 → `REJECTED` (`insufficient_balance`), `LIMIT` 주문 price 부재/zero notional/unknown order_type 모두 `REJECTED`, `mode`/`trading_mode` dict 필드가 `LIVE` 면 PAPER-only 거부, audit blob 에서 api_key/api_secret/access_token/passphrase/password/private_key 자동 sanitize. Upbit/OKX adapter 는 #21/#22 의 READ_ONLY stub 그대로 — `place_order` 호출 시 `ExchangeAdapterDisabledError`. 회귀: 53 케이스 (`tests/test_exchange_adapter.py`) — spec 메서드 존재 / orderbook bid<ask / LIMIT-SELL ACCEPTED / MARKET-BUY FILLED / cancel ACCEPTED / client_order_id dedup / 잔고 부족 / LIVE 거부 (mock + base) / audit secret 부재 / Upbit·OKX `place_order` disabled / Strategy·Agent·Market 모듈에서 `.place_order(`/`.cancel_order(`/`.fetch_balance(`/`.get_balance(` 직접 호출 부재 / strategies·agents 가 `app.brokers` import 부재 (compliance.py 만 예외 — meta-checker) / brokers 모듈 내 requests/httpx/hmac import 부재 / `ENABLE_LIVE_TRADING=True` literal 부재 / capability.to_dict() 값에 secret 문자열 부재. 문서: `docs/exchange_adapter_interface.md`. 본 단계 완료는 실거래 허가 아님 — #21/#22 Upbit/OKX 실주문은 별도 승격 절차. |
| 21 | Upbit Adapter | ✅ | (1차) `app/brokers/upbit_adapter.py` (READ_ONLY 영구 — pyupbit 공개 endpoint만, lazy import). 심볼 정규화. API 키 생성자에서 거부. 테스트는 fake client 주입 (네트워크 호출 0). **2026-05-18 확장**: 4개 보조 모듈 추가. `upbit_public.py` 가 transport-injection 기반 `UpbitPublicClient` 제공 — markets/ticker/orderbook/candles_minutes/trades_ticks 5개 public quotation 메서드, path 화이트리스트(`/v1/market/all`, `/v1/ticker`, `/v1/orderbook`, `/v1/candles/minutes/{unit}`, `/v1/trades/ticks`) 외 호출 시 차단, response parser 분리, transport 없으면 silent 네트워크 호출 대신 RuntimeError. `upbit_rate_limit.py` 가 `parse_remaining_req(header)` + `should_throttle(remaining, min_remaining)` + `RateLimitState`(sleep_fn 주입) 제공 — `group=market; min=599; sec=9` 같은 헤더 파싱 + 안전한 throttle 결정. `upbit_account.py` 가 `UpbitAccountClient` 제공 — credentials/transport 둘 중 하나라도 없으면 `UpbitAccountPermissionError`, secret 값은 보관/repr/응답 어디에도 노출 안 함, /v1/accounts 만 화이트리스트, 출금 메서드 부재. `upbit_order.py` 가 `UpbitOrderClient` disabled stub 제공 — `place_order`/`cancel_order`/`get_order` 모두 호출 시 `ExchangeAdapterDisabledError`, JWT/HMAC signing 코드 부재, credentials 인자도 폐기. `UpbitAdapter` 에 module-level `normalize_upbit_market`/`to_internal_symbol`/`is_krw_market` 헬퍼 + `public_client` 주입 옵션 (legacy pyupbit 경로 보존). 회귀 73 케이스 (`tests/test_upbit_adapter.py`) — public client transport injection / Remaining-Req 파싱 / throttle 결정 / 마켓 정규화(KRW/USDT/BTC) / account gated / order stub disabled / 출금 메서드 부재 / 주문 endpoint URL 부재 / brokers 내 requests·httpx·jwt·hmac import 부재 / Strategy·Agent 가 Upbit 모듈 import·인스턴스화 부재 / frontend 에 UPBIT 키 literal 부재. 문서: `docs/upbit_adapter.md`. 본 단계 완료는 실거래 허가 아님 — LIVE 주문은 별도 LIVE adapter + OrderGateway 승격 절차에서만. |
| 22 | OKX Adapter | ✅ | (1차) `app/brokers/okx_adapter.py` (READ_ONLY 영구 — ccxt 공개 endpoint, lazy import). 심볼 정규화. API key/secret/passphrase 거부. **2026-05-18 확장**: 4개 보조 모듈 추가. `okx_public.py` 가 transport-injection 기반 `OkxPublicClient` 제공 — instruments/ticker/orderbook/candles/funding-rate 5개 public endpoint, path 화이트리스트(`/api/v5/public/instruments`, `/api/v5/market/ticker`, `/api/v5/market/books`, `/api/v5/market/candles`, `/api/v5/public/funding-rate`) 외 차단, response parser 분리, transport 없으면 RuntimeError. `okx_rate_limit.py` — `parse_okx_api_error` + `is_okx_rate_limit_error` + `should_throttle_okx` + `OkxRateLimitState` (sleep_fn 주입). 코드 50011(Requests too frequent) 감지. `okx_account.py` — `OkxAccountClient` (key+secret+passphrase 셋 모두 필요, transport 별도 주입, secret 미보관, `/api/v5/account/balance`+`positions` 만 화이트리스트, 출금 메서드 부재). `okx_trade.py` — `OkxTradeClient` disabled stub (place/cancel/amend/get/leverage/margin-mode 전부 `ExchangeAdapterDisabledError`) + `OkxPaperOrderClient` (결정론적 spot/swap PAPER 엔진: MARKET FILLED + LIMIT ACCEPTED + 잔고 부족 REJECTED + `mode=LIVE` 거부 + client_order_id idempotent + leverage/margin/reduce_only 입력은 받되 *실제 미적용*, audit secret sanitize). OkxAdapter 에 module-level `normalize_okx_inst_id`/`infer_okx_inst_type`/`to_internal_symbol` 헬퍼 + `public_client` 주입 옵션 (legacy ccxt 경로 보존). 회귀 88 케이스(`tests/test_okx_adapter.py`) — instrument 정규화 / OPTION 거부 / SPOT vs SWAP 추론 / 50011 감지 / public client / account gated / trade disabled / paper spot+swap / Strategy·Agent 직접 호출 부재 / brokers 내 requests·httpx·jwt·hmac import 부재 / 실제 trade endpoint URL literal 부재 / `OK-ACCESS-SIGN` 코드 부재 / frontend 에 OKX 키 부재. 문서: `docs/okx_adapter.md`. 본 단계 완료는 실거래 허가 아님 — 실주문은 별도 LIVE class + OrderGateway 승격 절차에서만. |
| 23 | Binance Adapter | ✅ | (1차) `app/brokers/binance_adapter.py` (READ_ONLY 영구 — ccxt spot endpoint, lazy import). 심볼 정규화. API key/secret 거부. spot only — 선물은 #67. **2026-05-18 확장 (research/skeleton)**: 4개 보조 모듈 추가. `binance_public.py` 가 transport-injection 기반 `BinancePublicClient` 제공 — exchangeInfo/ticker.24hr/depth/klines/time 5개 public endpoint, path 화이트리스트(8개 path: `/api/v3/exchangeInfo`, `/api/v3/ticker/24hr`, `/api/v3/depth`, `/api/v3/klines`, `/api/v3/time`, `/api/v3/avgPrice`, `/api/v3/ticker/price`, `/api/v3/ticker/bookTicker`) 외 차단, native `BTCUSDT` 형식 강제(slash/dash 거부), response parser 분리, 권장 host `data-api.binance.vision` 상수화. `binance_rate_limit.py` — `X-MBX-USED-WEIGHT(-1M)` + `X-MBX-ORDER-COUNT-{10S,1M}` 헤더 파싱, `BinanceRateLimitState` (sleep_fn 주입, 1분 soft limit 기본 960=80% of 1200). `binance_account.py` — `BinanceAccountClient` 모든 메서드 즉시 `BinanceAccountPermissionError` (regulatory gate 통과 전 일체 차단, credentials 무관), secret 미보관, 출금 메서드 부재. `binance_trade.py` — `BinanceTradeClient` 7개 동작(place/cancel/get/set_leverage/set_margin_type/trade_futures/trade_margin) 모두 `ExchangeAdapterDisabledError`, `DISABLED_REASON="binance_live_trading_disabled_until_regulatory_review"` 상수, signing/HMAC 부재, 실제 trade endpoint URL literal 부재. `BinanceAdapter` 에 module-level `normalize_binance_symbol`/`to_internal_symbol`/`is_supported_binance_quote` 헬퍼 + `public_client` 주입 옵션 (legacy ccxt 경로 보존). 회귀 84 케이스(`tests/test_binance_adapter.py`) — symbol 정규화(default USDT, futures/perp 거부) / used-weight 헤더 / public client transport+화이트리스트 / account 모두 disabled / trade 모두 disabled + regulatory reason / Strategy·Agent 직접 호출 부재 / brokers 내 requests·httpx·jwt·hmac·python-binance/binance-connector import 부재 + `binance_public.py` 에 ccxt import 부재 / 실제 trade·account endpoint URL literal 부재 / `X-MBX-APIKEY` literal 부재 / frontend `BINANCE_API_KEY`/`BINANCE_SECRET_KEY` 부재. 문서: `docs/binance_adapter.md` research doc (규제/지역 제한 + Binance.US vs Global 구분). 본 단계 완료는 실거래 허가 아님 — Binance live 는 별도 phase + 규제·지역 제한 확인 후. |
| 24 | Mock Broker | ✅ | (1차) `MockExchangeAdapter` (`mock_broker.py`) — ExchangeAdapter contract 준수, 결정론적 가격, 잔고 차감, FILLED/ACCEPTED 응답. mode='PAPER' 영구 고정 — LIVE 키 받지 않음. **2026-05-18 확장 (simulation broker)**: `mock_simulation.py` 신규. `MockBroker` (top-level facade) + `MockBrokerConfig`(frozen — fee_bps/slippage_bps/allow_short/allow_margin/partial_fill_enabled/deterministic_seed/max_order_notional/mode∈{MOCK,PAPER}/initial_balances, LIVE 거부) + `MockAccountState`(다중 자산 free/locked, lock·unlock·settle_buy·settle_sell, allow_negative_base 숏 모델링) + `MockPositionBook`(가중평균 avg_entry_price, realized·unrealized PnL, 롱↔숏 전환 정확 회계) + `MockMarket`(set/get + hash fallback) + `MockExecutionEngine`(결정론 fee/slippage — random 사용 없음). 공개 API: `place_order(dict)→dict`(PaperBroker drop-in 호환), `cancel_order`, `get_order`, `get_balance(ccy)`, `get_position`, `get_account_summary`, `set_market_price`, `reset`. MARKET 즉시 FILLED + LIMIT crossable 즉시 체결·non-crossable open + open LIMIT 의 locked balance + cancel 시 자동 unlock + duplicate client_order_id idempotent + LIVE mode 거부(mode/trading_mode 필드) + allow_short=false 보유 초과 SELL REJECTED + allow_margin=false quote 잔고 초과 BUY REJECTED + max_order_notional 한도 + audit secret sanitize(api_key/secret/passphrase/ok_access_sign/x_mbx_apikey 등). 모든 응답 필수 키: `mode`, `is_real_trade=False`, `execution_source="mock_broker"`, `warning="Mock execution only. Not real profit or real trade."`. 회귀 51 케이스(`tests/test_mock_broker.py`) — 초기잔고/MARKET BUY+SELL/가중평균/LIMIT 4종/cancel locked 해제/fee+slippage/unrealized·realized PnL/max_order_notional/allow_short·allow_margin/dup client_order_id/LIVE 거부/invalid input/audit secret/reset/account summary/MockAccountState·MockPositionBook 단위/mock_simulation.py 에 requests·httpx·ccxt·pyupbit·binance·okx import 부재/Strategy·Agent 직접 호출 부재(compliance.py 예외)/PaperBroker 시그니처 호환. 문서: `docs/mock_broker.md`. 본 단계 완료는 실거래 허가 아님 — Mock 결과는 실제 수익·실체결 아님. |
| 25 | Paper Broker | ✅ | (1차) `PaperBroker` (`paper_broker.py`) — frozen dataclass 결과 + 슬리피지/수수료/미체결 시뮬, dict 인터페이스. **2026-05-18 확장**: read-only 시세 source 기반 paper 체결. `paper_market_broker.py` 신규 `PaperMarketBroker` + `PaperMarketBrokerConfig` (fee_bps/slippage_bps/allow_short/allow_margin/max_order_notional/require_source/universe whitelist/max_ticker_age_sec/initial_balances) + `PaperMarketSource` Protocol (MockExchangeAdapter/Upbit/OKX/Binance read-only adapter 모두 호환 — `fetch_ticker(symbol)` 만 사용). `paper_trader.py` 신규 `PaperTrader` 컨트롤러 — source 선택 (mock / upbit_readonly / okx_readonly / binance_readonly / kis_readonly_stub), `start_paper`/`stop_paper`/`reset_paper`, `select_paper_source`, `submit_paper_order_via_gateway(request, gateway)` 가 OrderGateway 경유 강제, `get_paper_status` + `get_paper_logs` (CRUD-lite, capacity-bounded). KIS adapter 는 미구현 → `kis_readonly_stub` 선택 시 source=None + warning 표시. REST 7개 endpoint (`app/api/paper.py`): GET `/api/paper/{status,orders,sources}` + POST `/api/paper/{start,stop,reset,source}` (admin token). 모든 응답에 `mode="PAPER"`, `is_real_trade=False`, `execution_source` (paper_broker/paper_trader), `warning="Paper execution only. Not real profit or real trade."`, `fill_quality_warning="Paper fills may differ from live execution..."` 포함. 단일 주문 경로 보존 — paper *주문 송신* 은 기존 OrderGateway 단일 경로로만 (별도 submit endpoint 없음). universe 밖 BUY → review_required REJECTED (EXIT 는 허용), stale ticker BUY 차단 (EXIT 허용 — #16 정책), LIVE mode 거부 (`mode`/`trading_mode` 필드 + PaperTrader 사전 차단), idempotent `client_order_id`/`idempotency_key`, audit secret sanitize. 회귀 46 케이스 (`tests/test_paper_broker.py`) — source 없음/MARKET·LIMIT·cancel/universe·staleness/fee+slippage/allow_short·margin/dup id/LIVE 거부/envelope/audit/PaperTrader source 선택+KIS warning+start·stop·reset/submit_via_gateway 위임·로그/REST API 7종 admin gating/정적 회귀(paper 모듈에 requests·httpx·ccxt·pyupbit·binance·okx import 부재 + strategies·agents 가 paper 모듈 직접 import/instantiate 부재) + MockExchangeAdapter 호환. 문서: `docs/paper_broker.md`. 본 단계 완료는 실거래 허가 아님 — Paper 결과는 실제 수익·실체결 아님. |
| 26 | API Rate Limit Guard | ✅ | (1차) `app/brokers/rate_limiter.py` (`TokenBucket` + `RateLimitExceeded`/`Timeout` + 거래소 프리셋 + `rate_limited` 데코레이터). 모의 시계 — 실제 sleep 0. **2026-05-18 확장**: 고수준 guard layer. `app/brokers/api_limits.py` 신규 — `RateLimitPolicy`(exchange/group/capacity/refill/safety_buffer/unit=req\|weight/cooldown_on_429·418·okx_50011·network/max_retries/base_backoff/max_backoff/disabled) + 거래소별 preset 10개 (upbit quotation·exchange-disabled / okx public·private-disabled·trade-disabled / binance spot_public weight 1200/min·spot_private-disabled·futures-disabled / mock·paper) + parse_retry_after + parse_okx_error + parse_upbit_remaining_req + parse_binance_used_weight wrapper. `app/brokers/rate_limit_guard.py` 신규 `RateLimitGuard` (`can_call`/`acquire`/`update_from_response`/`update_from_error`/`reset_retry`/`snapshot`/`reset`) + `GuardState`(remaining/used_weight/cooldown_until/consecutive_failures + 통계 9종) + `AcquireDecision` + `RetryDecision` + `ExchangeRateLimitRegistry` + `build_default_registry(preload=True)`. `ERROR_KIND_*` 8종 (429/418/okx_50011/upbit_too_many/network/auth/invalid/unknown). 무한 재시도 방지 — `max_retries` 초과 시 `should_retry=False`, exponential backoff (`base*2^attempt`, `max_backoff_sec` cap), `Retry-After` 헤더 우선, recursive retry 부재, `while True` 부재. `app/api/rate_limits.py` 신규 — `GET /api/rate-limits` (read-only snapshot, secret 미노출). 회귀 53 케이스 (`tests/test_rate_limit_guard.py`) — Policy validation/default policies/parser 4종/can_call·acquire/disabled/safety_buffer/cooldown 차단/response 헤더 자동 반영 (Upbit Remaining-Req·Binance X-MBX-USED-WEIGHT·OKX 50011 body)/update_from_error 429·418·50011·network·auth·invalid 별칭/max_retries 초과/무한 루프 방지/exponential backoff+cap/Retry-After 우선/reset/snapshot secret 부재/Registry get·register·snapshot_all·preload/REST API/금지 문자열 (`while True`/`ENABLE_LIVE_TRADING`/`requests`·`httpx`·`ccxt`·`pyupbit`·`binance`·`okx` import) 부재/Strategy·Agent 직접 import 부재. 문서: `docs/api_limits.md` — 거래소별 한도 정책 + 운영 전 공식 문서 재확인 필요 명시. 본 guard 자체는 HTTP/sleep 호출 안 함 — 정책 결정만. adapter 자동 연결은 후속 PR (기존 adapter 테스트 회귀 없음). |
| 27 | Secret Permissions | ✅ | (1차) `docs/api_key_policy.md` (3-tier 권한 모델 + 거래소별 권한 매핑 + 키 발급/저장 규칙 + 사고 대응). `test_api_key_policy.py` 회귀: ENABLE_WITHDRAWAL 영구 false / 모든 어댑터 출금 메서드 부재 / .env.example 비어있음 / 정책↔코드 인용 정확성. **2026-05-18 확장**: 정책 문서에 §8 거래소별 권한 상세 정책 (Upbit / OKX / Binance Global vs Binance.US) + §9 권한 체크리스트 통합 표 (MUST_OFF / DEFAULT_OFF / READ_LIMITED / TRADE_GATED 4분류, 15개 권한) + §10 스크린샷 보관 가이드 (마스킹 의무 7종, 파일명 규칙 `api-permission-<exchange>-<tier>-<YYYYMMDD>-redacted.png`, repository 밖 보관) + §11 `.env.example` 작성 원칙 (실제 키 금지, frontend VITE_/NEXT_PUBLIC_ secret 금지) + §12 운영 전 확인 절차 (9단계) + §13 위반 시 대응 (8단계 incident response) + §14 본 단계 범위 (실거래 허가 아님). `backend/app/audit/secret_masking.py` 신규 — `mask_secret`(None→`<unset>`, placeholder→`<placeholder>`, 짧은 값→`***`, 긴 값→`prefix***suffix`) + `mask_dict_values`. `scripts/check_secret_policy.py` 신규 — repository 정적 스캐너 (long secret assignment / inline secret / frontend VITE_·NEXT_PUBLIC_ secret / withdraw endpoint URL / withdraw 메서드 정의 / dangerous flag=true). 테스트 파일은 부정-예시 fixture 를 위해 일부 rule 에서 제외. exit code 0/1. 회귀 30 케이스 (`tests/test_secret_permissions_policy.py`) — mask_secret 7개 / 정책 문서 추가 phrase 11개 / .env.example 실제 키 부재 / dangerous flag default false / frontend secret literal 부재 (UPBIT·OKX·Binance·generic + VITE_·NEXT_PUBLIC_) / production withdraw 메서드·endpoint URL 부재 / ORM 컬럼 secret 부재 / Settings.enable_live_trading=False / ENABLE_WITHDRAWAL 환경변수 monkeypatch=true 도 false 유지 / AdapterCapability requires_secret 메타만 / logs 디렉터리 tracked secret 부재 / checklist 27 ✅ 표기 / 스캐너 스크립트 존재. 기존 `test_api_key_policy.py` 33 케이스 회귀 없음. backend 2034 passed / 1 skipped. |
| 28 | Sandbox/Paper Keys | ✅ | (1차) `docs/sandbox_paper_keys.md` 정책 + `Settings` 의 SANDBOX 키 슬롯 (OKX/Binance) + `Settings.validate()` 모드/키 정합성 경고 (PAPER에 LIVE키 / LIVE+SANDBOX 동시) + `.env.example` 분리 + 회귀 테스트. **2026-05-18 확장 (AppProfile + StartupGuard)**: `backend/app/core/profile.py` 신규 — `AppProfile`(PAPER/SHADOW/LIVE/TEST) + `KeyProfile` + `SecretClassification`(SAFE/PLACEHOLDER/TEST_LOOKING/REAL_LOOKING, Shannon entropy 기반) + `classify_secret_value`/`looks_like_real_secret`/`is_safe_secret` + `mask_secret`(#27 재export) + `GuardViolation` + `StartupGuardResult` + `validate_startup_profile(env)` (순수 함수) + `enforce_startup_profile(strict)` + `StartupGuardError`. 8개 검증 도메인: withdrawal 영구 금지 / PAPER (LIVE 키·ENABLE_LIVE_TRADING·LIVE_TRADING_MODE) / SHADOW (trade key·live flag) / LIVE (confirmation phrase `I_UNDERSTAND_LIVE_TRADING_RISK` 필수·enable flag·KEY_PROFILE=LIVE·sandbox-only key·sandbox-only flag·public env secret) / TEST (real-looking key·live flag) / TRADING_MODE 정합성. `backend/app/api/profile.py` 신규 `GET /api/profile` (read-only snapshot, 모든 secret `mask_secret` 으로 노출 — 평문 부재). `.env.example` 신규 변수 6개 (APP_PROFILE=PAPER/KEY_PROFILE=PAPER/LIVE_CONFIRMATION= 빈값/REQUIRE_LOCAL_SECRETS=true/ALLOW_SANDBOX_KEYS_ONLY=false/STARTUP_GUARD_STRICT=false). `.gitignore` 확장 — .env.live·.env.shadow·.env.paper·.env.paper.local·.env.test.local + backend/frontend 하위 동등 패턴. 회귀 53 케이스(`tests/test_env_profiles.py`) — Profile enums / Secret classification / PAPER·SHADOW·LIVE·TEST 각 violation rule / withdrawal parametrized 4-profile / public VITE_·NEXT_PUBLIC_*SECRET / strict mode StartupGuardError / masked summary 평문 부재 / REST API / .env.example 변수 + 기본값 검증 / .gitignore 패턴 / docs 존재 / 모듈 정적 회귀 (network import / strategies·agents 직접 import 부재). `docs/env_profiles.md` 신규 + api_key_policy.md / sandbox_paper_keys.md 에 cross-link. 본 단계 완료는 실거래 허가 아님 — strict 모드는 배포 스크립트 전용. |

## Phase 4 — 전략 (8)
| # | 항목 | 상태 |
|---:|---|:---:|
| 29 | StrategyBase | ✅ | (1차) `app/strategies/base.py` (`StrategyCapability` + `StrategyBase` Protocol + `StrategyRegistry` + `assert_signal_contract`). 4개 전략 클래스에 `capability` 클래스 속성 추가 (동작 변경 0). REST: `GET /api/strategies/catalog`. **2026-05-18 확장 (StrategyContract ABC)**: `app/strategies/contract.py` 신규 `StrategyContract` ABC (4개 abstract: `generate_signal`/`calculate_size`/`exit_rule`/`explain_signal`) + 읽기 전용 `StrategyContext`(symbol/timeframe/closes/highs/lows/volumes/freshness_ok/data_quality_grade/notice_context/theme_context/regime/positions_snapshot/extra — secret 키 차단) + `PositionSizingHint`(is_final_order_size=False 영구) + `ExitRuleDecision`(exit_qty_fraction 0..1, urgency normal/high/critical, is_order_intent=False 영구) + `SignalExplanation` + `evaluate()` 편의 메서드(contract 위반 시 `StrategyContractError`) + `ALLOWED_SIGNAL_ACTIONS` 6종 (BUY/SELL/HOLD/BLOCKED/NO_ACTION/WATCH_ONLY) + `assert_no_order_intent`/`is_safe_action`. `app/strategies/contract_registry.py` 신규 `ContractRegistry`(`StrategyContract` 하위만 등록, 중복 name 거부, regime/symbol/enabled 필터, `enabled_by_default=False` 기본). `app/agents/strategy_selection.py` 신규 hook — `StrategyActivationContext`/`StrategyActivationDecision`(direct_order_allowed=False 영구) + `select_active_strategies` (regime 매칭, pair 전략 단일 symbol 제외, notice/theme 차단 안 함). 회귀 42 케이스(`tests/test_strategy_contract.py`) — ABC instantiation / evaluate is_order_intent=True 시 raise / StrategyContext secret 키 거부 + list→tuple / PositionSizingHint·ExitRuleDecision·SignalExplanation 단위 / ContractRegistry 8개 / SelectionAgent hook 6개 / **정적 회귀** (app/strategies/ 가 app.brokers·execution·order_gateway import 부재 + .place_order/.cancel_order/.get_balance/.submit_order 호출 부재 + contract*.py 에 requests/httpx/ccxt/pyupbit/binance/okx SDK 부재 + ENABLE_LIVE_TRADING=True / is_order_intent: bool = True literal 부재 + strategy_selection agent brokers 미참조). 기존 35개 #29 1차 회귀 없음. 문서: `docs/strategy_base.md` 신규. 본 단계 완료는 실거래 허가 아님 — 30번 이후 개별 전략 구현은 본 작업 범위 아님. |
| 30 | Trend Following | ✅ | (1차) `app/strategies/trend_following.py` 분리 + `_indicators.py`(ema/sma/atr) + `_signals.py`. backward compat re-export. 14 회귀. **2026-05-18 확장 (StrategyContract ABC)**: `app/strategies/trend_following_contract.py` 신규 `TrendFollowingContractStrategy(StrategyContract)` — 4 abstract method 구현. `_indicators.py` 에 `true_range`/`donchian_channel(exclude_current=True)`/`adx`(Wilder smoothing) 추가. capability.name="trend_following_v2" + enabled_by_default=False + preferred_regimes=("TREND_UP","TREND_DOWN"). `generate_signal` 7개 가드(quality EXCLUDE/freshness stale/universe 외/high-risk notice/캔들 부족/OHLC mismatch/ADX 미달) → BLOCKED·NO_ACTION·HOLD, BUY candidate(close>SMA200 + EMA정배열 + Donchian breakout + ADX≥min), allow_short_candidates 옵트인. `calculate_size` base_notional×confidence (HOLD/BLOCKED/NO_ACTION 시 0), is_final_order_size=False 영구. `exit_rule` 6개 조건(quality EXCLUDE→critical/freshness→high/notice→high/EMA cross→0.6 normal/Donchian low 이탈→1.0 high/ADX 급락→0.3 normal), is_order_intent=False 영구. `explain_signal` summary="candidate only — not an order" + evidence(EMA·SMA·ADX·Donchian·data_quality·freshness·universe·regime) + risks + limitations. 회귀 45 케이스(`tests/test_trend_following_contract.py`) — Indicators 10 / Signal 10 / Sizing 3 / Exit 5 / Explanation 4 / evaluate 2 / **정적 가드** 4(broker·execution·SDK·order method·forbidden literal 부재) / Registry+SelectionAgent 5(TREND_UP 활성, RANGE 제외). 기존 1차 14 케이스 회귀 없음. 문서: `docs/trend_following.md` 신규. 본 단계 완료는 실거래 허가 아님 — 31번 이후 전략 구현은 본 작업 범위 아님. |
| 31 | Volatility Breakout | ✅ | (1차) `app/strategies/volatility_breakout.py` 분리. backward compat re-export 유지. capability + StrategyBase 만족. 13개 회귀 테스트. **2026-05-18 확장 (StrategyContract ABC)**: `app/strategies/volatility_breakout_contract.py` 신규 `VolatilityBreakoutContractStrategy(StrategyContract)` — 4 abstract method 구현. `VolatilityBreakoutParams` (14 파라미터, frozen dataclass): atr_period=14 / atr_avg_period=42 / range_lookback=25 / breakout_buffer_pct=0.002 / volume_surge_min=1.2 / require_volume_filter=False / vol_expansion_min=1.0 / high_vol_mult=3.0 / high_vol_size_shrink=0.5 / adx_max_for_breakout=0.0(=비활성) / min_candles_required=0 / allow_short_candidates=False / base_notional_usdt=100 / max_leverage_hint=1.0. capability.name="volatility_breakout_atr_v2" + enabled_by_default=False + preferred_regimes=("RANGE","TREND_UP","TREND_DOWN"). `generate_signal` 안전 가드 4(quality EXCLUDE/freshness stale/universe 외/high-risk notice → BLOCKED) + 데이터 가드 4(min_candles/range_lookback+1/OHLC mismatch/ATR_avg<=0 → NO_ACTION) + 필터 3(vol_expansion 미달/ADX 상한 초과/volume_surge 미달 → HOLD) + BUY/SELL candidate (현재 봉 제외 직전 N봉 max/min × buffer 돌파, `_compute_volume_ratio` extra.volume_ratio 우선 fallback volumes mean). `calculate_size` base×confidence + 초고변동(ATR>avg×3.0) 시 자동 50% 축소, is_final_order_size=False / used_for_order=False 영구. `exit_rule` 6 조건: quality EXCLUDE→critical full / freshness→high full / notice→high full / LONG·SHORT 모두 ATR×1.5 역행→0.7 partial normal / 변동성 축소(ATR/avg<0.5)→0.3 partial normal / 정상→should_exit=False, is_order_intent=False 영구. `explain_signal` summary="candidate only — not an order" + evidence(ATR/expansion ratio/recent_high/breakout_level/volume_ratio/data_quality/freshness/universe/regime) + risks(high-vol regime/stale/notice/theme review) + limitations. 회귀 39 케이스(`tests/test_volatility_breakout_contract.py`) — Signal 시나리오 5 / 안전 가드 4 / 데이터·필터 6 / Sizing 4(high_vol shrink 포함) / Exit 6(ATR×1.5 adverse·contraction 포함) / Explanation 4 / evaluate 1 / **정적 가드** 4 (broker·execution import / SDK import / order method 호출 / forbidden literal 부재) / Registry+SelectionAgent 5(RANGE 활성·UNKNOWN 보수적 통과). 기존 1차 13 케이스 회귀 없음. backend 2217 passed / 1 skipped. 문서: `docs/volatility_breakout.md` 신규. 본 단계 완료는 실거래 허가 아님 — 32번 이후 전략 구현은 본 작업 범위 아님. |
| 32 | Pair Trading | ✅ | (1차) `app/strategies/pair_trading.py` 분리 + `PairSignal` → `_signals.py`. `strategies.py` 가 단순 재export 허브로 축소(클래스/def 정의 0). 5경로 동일 객체 검증. 12개 회귀 테스트. **2026-05-18 확장 (StrategyContract ABC)**: `app/strategies/pair_trading_contract.py` 신규 `PairTradingContractStrategy(StrategyContract)` — 4 abstract method 구현. `PairTradingParams` (12 파라미터, frozen dataclass): window=60 / entry_z=2.0 / exit_z=0.5 / extreme_z=3.0 / min_correlation=0.6 / min_window_required=0(=window) / hedge_stability_window=20 / hedge_stability_max_drift=0.5 / base_pair_notional_usdt=100 / high_z_size_shrink=0.5 / risk_budget_low_z / risk_budget_high_z. capability.name="pair_trading_meanrev_v2" + supports_pair=True + enabled_by_default=False + preferred_regimes=("RANGE","MEAN_REVERSION","RELATIVE_VALUE"). pair 입력 — `StrategyContext.closes` = leg A, `extra["closes_b"]` = leg B, `symbol="A,B"` 또는 `extra["symbol_a"/"symbol_b"]`. `generate_signal`: 안전 가드 5(quality EXCLUDE/freshness stale/universe 외/leg high-risk notice → BLOCKED; min_correlation 미달 → BLOCKED; missing closes_b/window 부족/length mismatch/degenerate variance → NO_ACTION) → `|z|<exit_z` HOLD / `exit_z≤|z|<entry_z` WATCH_ONLY / `z≥entry_z` SELL (leg_bias=short_a_long_b) / `z≤-entry_z` BUY (long_a_short_b) / `|z|≥extreme_z` [extreme] 태그. **leg_bias 는 reason/evidence 설명 context** 일 뿐 action 으로 노출되지 않음 (CLAUDE.md §2.3 §3.1). `calculate_size`: base_pair_notional × confidence + `|z|≥extreme_z` 시 0.5 축소 + WARNING 시 0.7 추가 축소; HOLD/BLOCKED/NO_ACTION/WATCH_ONLY=0; **reason 에 "Final leg sizes ... decided by RiskManager / OrderGuard / PermissionGate" 명시**; is_final_order_size / used_for_order 영구 False. `exit_rule` 6 조건: quality EXCLUDE→critical full / freshness→high full / leg notice→high full / `|z|≤exit_z` (회귀 달성)→normal full / `corr<min_correlation` (가설 붕괴)→0.5 high / 진입 부호 반대+`|z|≥extreme_z`→0.7 normal; is_order_intent 영구 False. `explain_signal`: summary=candidate_pair_* + evidence(pair=(A,B)/hedge_ratio/spread_mean/spread_std/z_score/correlation/leg_bias/window/entry_z/exit_z/data_quality/freshness/universe/regime) + risks(corr drop/extreme z divergence/stale/EXCLUDE/WARNING/leg notice/theme review) + limitations(candidate only/leg_bias descriptive/RiskManager final/페어 *directionally neutral 아님*/BUY-SELL 후보 표현). 회귀 46 케이스(`tests/test_pair_trading_contract.py`) — Signal 시나리오 5 / 안전 가드 5 / 데이터 가드 4 / Sizing 6 / Exit 7 / Explanation 6 / evaluate 1 / **정적 가드** 5 (broker·execution import / SDK import / order method 호출 / forbidden literal / hedge leg order active pattern 부재 — `place_pair_order(`/`submit_leg_order(`/`app.order_gateway` import/`OrderGateway(`/`BrokerAdapter(` 인스턴스 생성 모두 부재) / Registry+SelectionAgent 7 (register/capability name/supports_pair=True/preferred_regimes/단일 symbol skipped+사유 명시/pair label 활성/UNKNOWN regime pair label 활성). 기존 1차 12 케이스 회귀 없음. backend **2263 passed / 1 skipped**. 문서: `docs/pair_trading.md` 신규 (20 sections incl. 방향성 리스크 한계, PAIR_DIVERGENCE_CANDIDATE 는 주문 아님, leg_bias 설명일 뿐 주문 지시 아님). 본 단계 완료는 실거래 허가 아님 — 33번 이후 전략 구현은 본 작업 범위 아님. |
| 33 | Kimp Mean Reversion | ✅ |
| 34 | Kimp Formula | ✅ | (1차) `app/market/kimp.py` float 단일 진리 소스 (`compute_kimp_pct(strict)` + `assess_kimp` + `breakeven_threshold_pct` + `expected_edge_pct` + `is_anomaly`). `KimpSnapshot.compute_kimp` (silent), `KimpMeanReversionStrategy.calculate_kimp` (strict) 가 본 모듈로 위임. 42개 회귀 테스트. **2026-05-18 확장 (Kimp Formula Calculator — Decimal 표준)**: `backend/app/market/kimp_calculator.py` 신규 — Decimal 기반 표준 계산 모듈 (전략·Agent 공통 단일 진리 소스 후속). `KimpCalculatorConfig`(8 파라미터, frozen Decimal: neutral_band_bps=5 / convergence_threshold_bps=10 / fx_rate_min=500 / fx_rate_max=3000 / fx_anomaly_deviation_bps=500 / structural_min_count=3 / structural_min_abs_bps=80 / direct_order_allowed=False 영구) + `KimpInputs`(domestic_price_krw / foreign_price_quote / fx_rate_krw / symbol / domestic_exchange / foreign_exchange / quote_currency / timestamp / previous_premium_bps / reference_fx_rate_krw) + `KimpResult`(foreign_price_krw / premium_ratio / premium_percent / premium_bps / direction / convergence_state / delta_bps / fx_anomaly / fx_anomaly_reason / fx_deviation_bps / is_valid / invalid_reason / risk_flags / computed_at / direct_order_allowed=False 영구). 상태 라벨: `Direction`(KIMP/REVERSE_KIMP/NEUTRAL) / `ConvergenceState`(EXPANDING/CONVERGING/NEUTRAL/UNKNOWN) / `DislocationKind`(STRUCTURAL/TEMPORARY/MIXED/UNKNOWN). 공식 (Decimal): `foreign_price_krw = foreign × fx`, `premium_ratio = (domestic - foreign_krw)/foreign_krw`, `premium_percent = ratio×100`, `premium_bps = ratio×10_000`. `compute_kimp(inputs, *, config=None)`: 유효성 검사 (domestic/foreign/fx > 0) → invalid 시 risk_flags=("invalid_input",) + premium=0 / 유효 시 direction(|bps|≤neutral_band → NEUTRAL, 양수 KIMP/음수 REVERSE_KIMP) + convergence(previous None→UNKNOWN, |delta|≤threshold→NEUTRAL, +초과→EXPANDING, -초과→CONVERGING) + fx_anomaly(sanity range 또는 reference 대비 deviation_bps 초과). `calculate_fee_adjusted_premium_bps(raw, *, domestic_fee_bps, foreign_fee_bps, fx_fee_bps, transfer_cost_bps, funding_bps)`: `sign(raw) × max(0, |raw| - total_cost)` (funding 은 abs), 0 으로 clamp + 부호 보존 — *참고 보정값* (거래 가능성 보장 안 함). KimpAgent hook 최소 준비: `build_kimp_context(result)` (평탄 dict, Decimal→str 직렬화, direct_order_allowed=False 명시) + `classify_structural_vs_temporary_dislocation(results, *, config=None)` (is_valid 결과만 사용, 개수 < structural_min_count → UNKNOWN, 부호 일관+평균|bps|≥structural_min_abs_bps → STRUCTURAL, 부호 혼재 → TEMPORARY, 부호 일관+평균 미달 → MIXED, 모든 경우 direct_order_allowed=False). 회귀 43 케이스 (`tests/test_kimp_calculator.py`) — 공식 정확성 5 / 유효성 3 / FX anomaly 4 / 수렴·확대 4 / fee-adjusted 5 / KimpAgent hook 1 / dislocation 4 / Decimal 정밀도 2 / 단일 진리 소스 호환 2 (기존 float `compute_kimp_pct` 값 일치 + KimpMeanReversionStrategy 회귀) / **정적 가드** 8 (broker·execution import 부재 / order_gateway·adapter import 부재 / 전략 모듈 import 부재 / network SDK import 부재 / order method 호출 부재 / forbidden literal 부재 / direct_order_allowed 영구 False 확인 / KimpResult·Inputs·Config frozen). 기존 #34 1차 42 케이스 회귀 없음. backend 회귀 (#29~#36 + #32) **328 passed / 0 failed**. KimpStrategy 코드 직접 수정 없음 (기존 동작 보호). 문서: `docs/kimp_formula.md` 신규 (16 sections incl. raw vs adjusted 구분 / direct_order_allowed=False / 주력 상시전략 금지). 본 단계 완료는 실거래 허가 아님 — 35번 이후 작업은 본 작업 범위 아님. |
| 35 | Kimp Guards | ✅ | (1차) `app/strategies/kimp_guards.py` (`GuardResult`/`EntryGuardsReport` + 7개 순수 함수 가드 + `evaluate_entry_guards` 7단계 평가). KimpStrategy 가 본 모듈에 위임. severity (`pass`/`hold`/`block`) 분리로 HOLD/BLOCKED 구분 명확. 새 `kimp_anomaly` 가드 추가 (±10% 이상치 차단). 40개 회귀 테스트. **2026-05-18 확장 (Kimp Risk Guards — 구조적 GuardDecision)**: `backend/app/strategies/kimp_risk_guards.py` 신규 — 8개 가드 합성 + `KimpGuardDecision` API. `KimpGuardConfig`(15 파라미터, frozen Decimal/int: max_fx_age_seconds=60 / max_price_age_seconds=30 / max_orderbook_age_seconds=10 / min_bid_size=0 / min_ask_size=0 / max_spread_bps=50 / require_orderbook_context=False / block_reverse_kimp_short_in_bull_market=True / bull_market_regimes=("STRONG_BULL","BULL_TREND") / bull_market_themes=("ETF_INFLOW","MARKET_WIDE_RALLY","RISK_ON_STRONG") / funding_risk_threshold_bps=100 / max_funding_age_seconds=600 / require_funding_context=False / require_notice_context=True / block_on_data_quality_exclude=True / block_on_data_quality_warning=False / direct_order_allowed=False 영구 / used_for_order=False 영구) + `KimpGuardInput`(symbol/intended_kimp_state/domestic_exchange/foreign_exchange/notices(듀크 타입 dict)/notice_context_available/fx_rate_krw/fx_timestamp/fx_reference/fx_source/kimp_result(#34)/도메스틱+해외 bid/ask/bid_size/ask_size/orderbook_timestamp/market_regime/theme_tags/short_leg_implied/funding_rate_pct/funding_timestamp/funding_position_side/도메스틱+해외 price_timestamp/data_quality_grade/now) + `KimpGuardReason`(code/severity/source/message/exchange/symbol/evidence) + `KimpGuardDecision`(allowed/required_review/recommended_action/reasons/blocked_by/review_codes/computed_at/direct_order_allowed=False 영구/used_for_order=False 영구) + 라벨: `GuardSeverity`(INFO/WARNING/HIGH/CRITICAL) / `GuardSource`(notice/fx/liquidity/bull_market/funding/freshness/data_quality/missing_context) / `RecommendedAction`(ALLOW_CANDIDATE/CONTEXT_ONLY/REVIEW_REQUIRED/BLOCK_CANDIDATE) / `KimpCandidateState`(REVERSE_KIMP_CANDIDATE/KIMP_CANDIDATE/NEUTRAL_CANDIDATE/UNKNOWN) / `GuardCode`(23개 코드). 8개 가드: `check_notice_risk` (deposit/withdrawal/delisting/caution/trading_suspension 매핑 + 미매핑 HIGH severity + global exchange notice + missing context) / `check_fx_risk` (fx≤0 CRITICAL 조기반환 / fx_source missing WARNING / fx_stale HIGH / KimpResult.fx_anomaly HIGH) / `check_liquidity_risk` (양 leg orderbook_missing/orderbook_invalid/liquidity_thin/spread_wide/orderbook_stale) / `check_bull_market_short_risk` (REVERSE_KIMP+short_leg+bull_regime/bull_theme → HIGH block, intended_state/short_leg/config 토글로 통과) / `check_funding_risk` (rate threshold/방향 불리/stale/missing) / `check_freshness_risk` (도메스틱+해외 price stale HIGH / 둘 다 None WARNING) / `check_data_quality_risk` (EXCLUDE CRITICAL / WARNING 기본 review-only / 토글로 HIGH 승격) / `check_missing_critical_context` (fx_rate_krw + kimp_result + intended_kimp_state 누락 보충). 합성 `evaluate_kimp_guards`: blocking severity (HIGH/CRITICAL) 분리 → blocked_by 있으면 BLOCK_CANDIDATE / review_codes 만 있으면 REVIEW_REQUIRED / 없으면 ALLOW_CANDIDATE. KimpAgent hook `build_kimp_guard_context(decision)`: 평탄 dict + direct_order_allowed/used_for_order=False 명시 + Decimal/datetime str 직렬화 + reasons flat list. 회귀 52 케이스 (`tests/test_kimp_risk_guards.py`) — 합성 정책 3 / 공지 9 / FX 5 / 호가 6 / 강세장 short 5 / 펀딩 5 / freshness 3 / 데이터 품질 3 / missing context 2 / KimpAgent hook 2 / **정적 가드** 7 (broker·execution import 부재 / order_gateway·adapter import 부재 / network SDK import 부재 / order method 호출 부재 (.place_order/.cancel_order/.get_balance/.submit_order/.withdraw/.deposit) / forbidden literal 부재 / "BUY"/"SELL"/"ENTER"/"EXIT" quoted 리터럴 부재 — docstring 설명용 단어는 허용 / direct_order_allowed/used_for_order 영구 False / frozen dataclass) / **#35 1차 backward compat** 1 (`evaluate_entry_guards` 회귀). 기존 #35 1차 40 케이스 회귀 없음. backend 전체 회귀 **2358 passed / 1 skipped** (이전 2306 + 신규 52). KimpStrategy 코드 직접 수정 없음 (기존 동작 보호). 문서: `docs/kimp_guards.md` 신규 (9 sections incl. 가드별 정책 표, KimpAgent hook 출력 형태, 직접 주문 금지 검증 카탈로그). 본 단계 완료는 실거래 허가 아님 — 36번 이후 작업은 본 작업 범위 아님. |
| 36 | Funding Cost Guard | ✅ | (1차) `app/market/funding.py` (기여도/누적/연환산/이상치/방향). `kimp_guards.guard_funding_extreme` + `guard_funding_direction` + `evaluate_entry_guards` 가 8단계로 확장 (funding_extreme 가드 포함). KimpStrategy 가 funding_rate_pct 를 가드에 전달. 40개 회귀 테스트. **2026-05-18 확장 (FundingCostGuard — 구조적 Decision API)**: `backend/app/risk/funding.py` 신규 — 진입/보유 평가 분리 `FundingGuardDecision` 합성. `FundingGuardConfig`(10 frozen 파라미터: funding_interval_hours=8 / max_funding_age_seconds=600 / extreme_threshold_bps=100 / review_ratio=0.4 / block_ratio=0.8 / accumulated_cost_warning_pct=1.0 / accumulated_cost_reduce_pct=2.0 / require_funding_context=True / direct_order_allowed=False 영구 / used_for_order=False 영구) + `FundingRateSnapshot`(rate_pct/timestamp/interval_hours/exchange/symbol — 거래소별 funding interval 노출) + `FundingCostInput`(symbol/side/snapshot/intended_hours_held/expected_edge_pct/is_held/accumulated_funding_cost_pct/holding_hours_so_far/now) + `FundingCostEstimate`(cost_pct signed/abs_cost_pct/cost_bps/annualized_pct/num_funding_events/cost_to_edge_ratio/is_unfavorable/rate_pct/side/interval_hours) + `FundingGuardReason`(code/severity/source/message/evidence) + `FundingGuardDecision`(input/estimate/allowed/required_review/recommended_action/reasons/blocked_by/review_codes/computed_at/mode("entry"/"hold")/direct_order_allowed=False 영구/used_for_order=False 영구) + 라벨: `GuardSeverity`(INFO/WARNING/HIGH/CRITICAL) / `GuardSource`(funding_data/funding_cost/funding_direction/funding_accumulated/missing_context) / `RecommendedAction`(ALLOW_NEW_CANDIDATE/BLOCK_NEW_CANDIDATE/REVIEW_REQUIRED/HOLD_CANDIDATE/REDUCE_CANDIDATE — *BUY/SELL/ENTER/EXIT 토큰 없음*) / `GuardCode`(11개 코드). 핵심 계산 (Decimal): `num_funding_events = intended_hours_held / interval_hours`, `signed_per_event = -rate (short) / +rate (long) / abs(rate) (unknown)`, `signed_cost = per_event × events`, `annualized = rate × (24/interval × 365)`, `cost_to_edge_ratio = abs_cost / |edge|`. `compute_funding_estimate(input, config=None)` 단독 호출 가능. `evaluate_funding_entry`: missing-critical(side/symbol) HIGH → funding_data_missing (require_funding_context 토글) → funding_invalid_interval HIGH → funding_data_stale HIGH (timestamp None 또는 age > 600s) → funding_extreme HIGH (|rate × 100| > 100 bps) → estimate 산출 후 funding_direction_adverse WARNING (is_unfavorable) → cost_to_edge_ratio 정책 (ratio≥0.8 HIGH FUNDING_COST_EXCEEDS_EDGE / 0.4≤ratio<0.8 WARNING FUNDING_COST_NEAR_EDGE / 그 외 PASS) → 합성 ALLOW_NEW_CANDIDATE/REVIEW_REQUIRED/BLOCK_NEW_CANDIDATE. `evaluate_funding_hold`: 동일 데이터/방향 가드 + 추가 funding_accumulated_reduce HIGH (≥2.0% reduce) / funding_accumulated_high WARNING (≥1.0% review) → 합성 HOLD_CANDIDATE/REVIEW_REQUIRED/REDUCE_CANDIDATE. `FundingCostGuard` 클래스: config 묶음 + `estimate`/`evaluate_entry`/`evaluate_hold` 메서드. KimpAgent / RiskManager hook `build_funding_guard_context(decision)`: 평탄 dict + direct_order_allowed/used_for_order=False 명시 + Decimal/datetime str 직렬화 + estimate flat dict. 회귀 45 케이스 (`tests/test_funding_guard.py`) — 합성 정책 5 / 데이터 가드 6 / extreme 2 / 방향 signed cost 5 (short+/short-/long+/long-/unknown) / 비용 계산 5 (num_events/annualized/cost_bps identity/cost_to_edge_ratio/None edge) / cost_to_edge 정책 4 / hold 평가 4 / missing context 2 / 클래스 1 / KimpAgent hook 2 / **정적 가드** 8 (broker·execution import 부재 / order_gateway·adapter import 부재 / network SDK 부재 / order method 호출 부재 (.place_order/.cancel_order/.get_balance/.submit_order/.withdraw/.deposit/.set_leverage/.set_margin) / forbidden literal 부재 / "BUY"/"SELL"/"ENTER"/"EXIT" quoted 리터럴 부재 / direct_order_allowed+used_for_order 영구 False / frozen dataclass) / **#36 1차 backward compat** 1 (annualized_funding_rate_pct + is_extreme_funding + is_funding_unfavorable 회귀). 기존 #36 1차 40 케이스 회귀 없음. backend 전체 회귀 **2403 passed / 1 skipped** (이전 2358 + 신규 45). KimpStrategy / kimp_risk_guards 코드 직접 수정 없음 (기존 동작 보호). 문서: `docs/funding_cost_guard.md` 신규 (14 sections incl. long/short funding 방향, annualized 공식, cost_to_edge_ratio 임계, 거래소별 interval 차이, 직접 주문 금지 카탈로그). 본 단계 완료는 실거래 허가 아님 — 37번 이후 작업은 본 작업 범위 아님. |

## Phase 5 — Agent (10)
| # | 항목 | 상태 |
|---:|---|:---:|
| 37 | Agent Architecture | ✅ | `app/agents/{base,anomaly,signal_quality,risk_officer,orchestrator}.py` (AgentBase Protocol + AgentCapability + AgentRegistry + 4 sub-agent). Orchestrator 가 4단계 파이프라인 위임 + `decide_with_pipeline` 단계별 보고. RiskOfficer 최종 거부권 + WATCH_ONLY 도입. REST: `GET /api/agents/catalog`. |
| 38 | Risk Officer Agent | ✅ | RiskOfficerAgent + 신규 가드 (emergency_stop / position 한도 / order notional / leverage). `risk_context_from_manager(rm, order, account)` 헬퍼로 RiskManager 통합. ENTRY 액션만 포지션 한도 적용. 32개 회귀 테스트 (e2e 포함). |
| 39 | Signal Quality Agent | ✅ | `SignalQualityAgent` boosted — `QualityBreakdown` 분해 + QualityReport(#17)/news_severity(#19)/freshness/kimp_anomaly_hint 통합. block 뉴스 -30 / warn -10 / freshness stale -10. 32개 회귀 테스트. |
| 40 | Anomaly Agent | ✅ | `AnomalyAgent` boosted — Quality(#17)/Notices(#18)/News(#19)/Kimp(#34) 통합 hard veto. `anomaly_context_for(symbol, exchange, *, notices, themes, news, quality_report, freshness_stale, kimp_pct)` 헬퍼. SignalQualityAgent 의 -30 페널티와 별개로 block 뉴스 즉시 차단. 32개 회귀 테스트. |
| 41 | Explain Agent | ✅ | `ExplainAgent` — `explain_signal`/`explain_decision`/`explain_pipeline` (short/full/markdown 포맷). 액션 한국어 라벨 매핑 (BUY→매수, OPEN_REVERSE_KIMP→역김프 진입 후보). risk_veto 시 ⛔ 표시. ctx 보조 정보 (regime/themes/volume_surge/freshness/kimp). 34개 회귀 테스트. |
| 42 | Daily Report Agent | ✅ | `DailyReportAgent` — AuditLog 이벤트 집계 → `DailyReport(OrderSummary, AgentSummary, key_events)`. 시간 범위 필터 (since/until, 기본 오늘 자정). markdown/plain 렌더링. 28개 회귀 테스트 (e2e 시나리오 포함). |
| 43 | Theme Insight Agent | ✅ | `ThemeInsightAgent` — 심볼별 테마/뉴스/공지/김프 통합 브리핑. `SymbolBriefing` (themes/news_severity/headlines/tradable/notice_reasons/kimp_anomaly/overall_outlook). markdown/plain 렌더링 + outlook 이모지. 31개 회귀 테스트. |
| 44 | Loss Tagging Agent | ✅ | `LossTaggingAgent` — `TradeOutcome` → `LossAnalysis` (10개 카테고리: STOP_LOSS/TIME_STOP/SLIPPAGE/SPREAD/REGIME_CHANGE/KIMP_DIVERGENCE/NEWS_SHOCK/FUNDING_BURN/FEE_HEAVY/UNKNOWN). primary + contributing 태그. markdown/plain 렌더링. 35개 회귀 테스트. |
| 45 | Performance Agent | ✅ | `PerformanceAgent` — 거래 시퀀스 → `PerformanceMetrics` (총 거래/승률/PnL/Profit Factor/Max Drawdown/best/worst). by_strategy 분해, by_loss_category (LossTaggingAgent 통합). window 슬라이싱. inf PF/MDD 정확 처리. 35개 회귀 테스트. |
| 46 | Compliance Agent | ✅ | `ComplianceAgent` — CLAUDE.md 안전 원칙 자동 점검. 9개 표준 check (ENABLE_WITHDRAWAL/출금 메서드/redaction/AgentDecision/RiskOfficer/모듈 경계/legacy import/frontend secrets/feature flag). Settings.validate() 통합. fatal/warning 분리 + 🟢🟡🔴 emoji 렌더링. 28개 회귀 테스트. |

## Phase 6 — 리스크/주문실행 (13)
| # | 항목 | 상태 |
|---:|---|:---:|
| 47 | RiskManager | ✅ |
| 48 | Position Limit | ✅ (manager 내부) |
| 49 | Loss Limit | ✅ (manager 내부) |
| 50 | Kill Switch | ✅ (manager + API) |
| 51 | Order Guard | ✅ | `app/execution/order_guard.py` (`OrderGuard` + `OrderGuardResult`). 7개 검사 — 필수 필드/notional/leverage/action 화이트리스트/symbol 형식+blacklist/source 화이트리스트. OrderGateway 가 RiskManager 통과 후 호출. 53개 회귀 테스트. |
| 52 | AI Permission Gate | ✅ |
| 53 | Order Gateway | ✅ |
| 54 | OrderExecutor | ✅ | `app/execution/order_executor.py` (`OrderExecutor` Protocol + `PaperExecutor`/`ShadowExecutor`/`LiveExecutor`). OrderGateway 가 route 별 dict 로 위임. 외부 주입 가능 (테스트 mock). |
| 55 | Manual Approval | ✅ |
| 56 | PaperTrader | ✅ |
| 57 | Live Shadow | ✅ | `ShadowExecutor` 구현 — 주문 송신 없이 audit 만 기록. LIVE_SHADOW 모드에서 PermissionGate 가 route='shadow' 결정 시 호출. |
| 58 | AI Assist | ✅ | `ApprovalItem` 에 `source`/`agent_explain` 필드 추가. `ApprovalQueue.add(source=..., agent_explain=...)` + `pending_by_source('ai')` 필터. OrderGateway 가 source='ai' 를 ApprovalItem 에 패스스루. |
| 59 | AI Execution Gate | ✅ | `app/risk/ai_execution_gate.py` (`AIExecutionGate` + `AIGateResult`). 4개 임계값: `min_confidence`(0.75)/`min_quality_score`(80)/`max_daily_orders`(50)/`per_symbol_cooldown_sec`(15분). OrderGateway 가 source='ai' + route='live' 시에만 호출. `record_executed` 후 카운터/쿨다운 갱신. |

## Phase 7 — 백테스트/승격 (7)
| # | 항목 | 상태 |
|---:|---|:---:|
| 60 | Backtest Engine | ✅ | `app/backtest/engine.py` (`BacktestRunner` + `BacktestBar`/`BacktestSignal`/`BacktestResult`). 단일 포지션 이벤트 루프, 슬리피지/수수료, equity curve, TradeOutcome 출력. |
| 61 | Metrics | ✅ | `app/backtest/metrics.py` `compute_metrics` — `PerformanceAgent` 위임 + equity curve MDD + sharpe-like. `BacktestMetrics` 데이터 클래스. |
| 62 | Walk-forward | ✅ | `app/backtest/walk_forward.py` (`WalkForwardRunner` — expanding/rolling 모드, N-fold, min_fold_bars). 폴드별 metrics + avg_*. |
| 63 | Monte Carlo | ✅ | `app/backtest/monte_carlo.py` (`MonteCarloRunner` — bootstrap with replacement, seed 결정론, p05/p50/p95 percentile). |
| 64 | Promotion Gate | ✅ | `check_manual_approval_gate` (LIVE_MANUAL → LIVE_AI_ASSIST) — 6개 기준 (운영 기간/승인 건수/응답 시간/거부율/연속 손실/Compliance). |
| 65 | Paper Gate | ✅ (`check_paper_gate`) |
| 66 | AI Assist Gate | ✅ | `check_ai_execution_gate` (LIVE_AI_ASSIST → LIVE_AI_EXECUTION) — 6개 기준 (AI 보조 기간/처리 건수/사람 오버라이드율/AI Sharpe/MDD/Compliance). |

## Phase 8 — 선물 (6)
| # | 항목 | 상태 |
|---:|---|:---:|
| 67-72 | Futures Scope/Adapter/Margin/Strategy/UI/Gate | ☐ (의도적 후순위) |

## Phase 9 — 웹/PWA/배포 (11)
| # | 항목 | 상태 |
|---:|---|:---:|
| 73 | Agent-first Dashboard | ✅ | DashboardPage 가 StatusCard + KillSwitchButton + ApprovalQueueWidget 통합. 5초 폴링으로 라이브 상태 표시. |
| 74 | Approval UI | ✅ | `pages/ApprovalsPage.tsx` + `api/approvals.ts` + `components/ApprovalQueueWidget.tsx`. 승인/거부 + source 필터 (all/ai/manual) + AI agent_explain 표시. |
| 75 | Risk Panel | ✅ | `pages/RiskPage.tsx` — 시스템 상태/Kill Switch/safety_warnings(#9)/승격 게이트 visual. `api/risk.ts` (kill_switch + promotion gates). |
| 76 | PWA | ✅ | `public/manifest.json` + `public/sw.js` (cache-first 정적 / network-only `/api/*`). `index.html` 에 manifest 링크 + theme-color. main.tsx 에서 production 빌드만 SW 등록. |
| 77 | Notifications | ☐ | (별도 PR 권장 — Telegram 통합은 §2.1 secret 규제와 신중 검토) |
| 78 | Frontend Integration | ✅ | 전체 UI 가 backend `/api/*` 와 통합 (status/freshness/watchlist/audit/approvals/risk). 5초/60초 폴링. |
| 79 | Pages Demo | ◕ | `docs/deployment_github_pages_demo.md` 존재. mock data 모드는 후속. |
| 80 | Admin Login | ✅ | `contexts/AdminTokenContext.tsx` localStorage 기반 + `components/AdminTokenInput.tsx` 헤더 우측. 비-secret bundle 보장. |
| 81 | Tailscale | ◕ | `docs/deployment_mobile_tailscale.md` 존재. 실제 배포 검증은 운영자 수동 단계. |
| 82 | Tauri | ☐ | (별도 PR — 데스크탑 빌드는 노드/Rust 의존성 큼) |
| 83 | Auto Update | ✅ | `components/VersionWatcher.tsx` — `/api/app` 60초 폴링, 새 버전 감지 시 알림 배너. 자동 reload 안 함 (작업 중 방해 방지). |

## Phase 10 — 테스트/운영 (10)
| # | 항목 | 상태 | 산출물 |
|---:|---|:---:|---|
| 84 | Unit Tests | ✅ | pytest 1302 통과 / 1 skip (2026-05-15) |
| 85 | Integration Tests | ◔ | `backend/tests/integration/test_e2e_kimp_signal_to_audit.py` 1건 — 추가 시나리오 후속 |
| 86 | Staging | ☐ | (실거래 승격 단계 — paper-only 운영 중에는 비활성) |
| 87 | Audit Log | ◕ | 메모리 + CSV + redaction 자동 적용. DB 영속화는 `AuditEvent` 모델 존재, 라우팅 통합 후속 |
| 88 | Backup | ✅ | `scripts/backup_audit.py` (logs/ → 타임스탬프 폴더 + keep-days 보존) + 회귀 테스트 |
| 89 | Monitoring | ✅ | `/api/metrics` (JSON) + `/api/metrics/prom` (Prometheus) + `/api/healthz` |
| 90 | MVP Gate | ✅ | `scripts/mvp_gate.py` — compliance/required_docs/frontend_dist + `--skip-tests` 옵션. 2026-05-15 PASS |
| 91 | Pre-market Checklist | ✅ | `scripts/pre_market_checklist.py` — fatal/warning 분리, JSON 출력. 2026-05-15 9/10 통과 (ADMIN_TOKEN 기본값 warning) |
| 92 | Release Notes | ✅ | `docs/release_notes_template.md` (추가/개선·안전·정책·호환성·알려진 이슈·검증 섹션) |
| 93 | Security Scan | ✅ | `scripts/security_scan.py` (6개 정규식 패턴 + `# noqa: security-scan` per-line opt-out). 2026-05-15 0 finding |

## Phase 11 — 분석 고도화 (3)
| # | 항목 | 상태 |
|---:|---|:---:|
| 94-96 | Alpha Decay / Correlation Guard / Loss Tagging | ☐ |

---

## Step A 완료 사항 (2026-05-10)
- 디렉토리 정렬: `audit/`, `governance/`, `brokers/`, `schemas/`, `db/`, `api/` 신설
- `storage/` → `audit/`, `promotion/` → `governance/`, `risk/approval_queue` → `execution/`, `execution/paper_broker` → `brokers/`, `market/models` → `schemas/market` 이동
- `main.py` → `api/` 라우터 분리 (slim main + 7 라우터)
- 격리: `backend/_legacy_innogrit/` (분석/포지션매니저/utils/trade_managers/exit_engine/websocket_feed/quotes_guard)
- 신규: `CLAUDE.md`, `core/feature_flags.py`, `core/app_info.py`, `brokers/{base,mock_broker}.py`, 4개 smoke 테스트
- 56/56 테스트 통과 (모듈 경계 + 기본 false 회귀 포함)

## 다음 작업 우선순위 (Step B 진행 중)
- [x] **#1 Product Scope** — 완료 (2026-05-10)
- [x] **#2 Strategy Portfolio** — 완료 (2026-05-10)
- [x] **#3 Operating Modes** — 완료 (2026-05-10)
- [x] **#4 Safety Principles** — 완료 (2026-05-10)
- [x] **#5 Agent Trader Naming** — 완료 (2026-05-10) — **Phase 0 (방향/범위) 5/5 전체 완료**
- [x] **#8 Shared Schemas** — 완료 (2026-05-10) — signal/order/position/risk/agent 단일 진입점 + is_order_intent 강제
- [x] **#11 Audit Foundation** — 완료 (2026-05-10) — redaction + OrderAuditLog + AgentDecisionLog
- [x] **#13 Database Schema** — 완료 (2026-05-10) — SQLAlchemy 2.0 + Alembic + 4 테이블
- [x] **#14 Watchlist/Universe** — 완료 (2026-05-10) — watchlist 테이블 + Service + REST API
- [x] **#7 Frontend Skeleton** — 완료 (2026-05-10) — Vite/React/TS, 3 페이지, 4 API 모듈
- [x] **#15 Market Data Collector** — 완료 (2026-05-10) — collector + Mock source + REST + freshness 통합
- [x] **#17 Data Quality** — 완료 (2026-05-10) — quality.py + CLI script
- [x] **#18 Exchange Notices** — 완료 (2026-05-10) — notices.py + REST + KimpStrategy 직결 플래그
- [x] **#19 Trend/News/Theme Signals** — 완료 (2026-05-10) — themes.py + classify_regime + 통합 컨텍스트
- [x] **#9 Config Layer** — 완료 (2026-05-10) — summary/validate + REST + .env.example 파리티
- [x] **#20 Exchange Adapter Interface** — 완료 (2026-05-10) — capability 모델 + MockExchangeAdapter + 출금 부재 회귀
- [x] **#24 Mock Broker** — 완료 (2026-05-10) — MockExchangeAdapter (ExchangeAdapter 풀 구현)
- [x] **#21 Upbit Adapter** — 완료 (2026-05-10) — READ_ONLY 영구, pyupbit 공개 API, lazy import
- [x] **#22 OKX Adapter** — 완료 (2026-05-10) — READ_ONLY 영구, ccxt 공개 endpoint, lazy import
- [x] **#23 Binance Adapter** — 완료 (2026-05-10) — READ_ONLY 영구, ccxt spot, native 심볼 분리 지원
- [x] **#26 API Rate Limit Guard** — 완료 (2026-05-10) — TokenBucket + 프리셋 + 데코레이터
- [x] **#27 Secret Permissions** — 완료 (2026-05-10) — api_key_policy.md + 회귀 테스트
- [x] **#28 Sandbox/Paper Keys** — 완료 (2026-05-10) — sandbox_paper_keys.md + Settings SANDBOX 슬롯 + validate 정합성

**Phase 3 (브로커/API) 9/9 ✅ 전체 완료**
- [x] **#29 StrategyBase** — 완료 (2026-05-10) — capability + Protocol + Registry + catalog API
- [x] **#30 Trend Following** — 완료 (2026-05-10) — trend_following.py + _indicators + _signals
- [x] **#31 Volatility Breakout** — 완료 (2026-05-10) — volatility_breakout.py 분리; 2026-05-18 확장 — `volatility_breakout_contract.py` ABC 구현 (ATR 확장 + Donchian-style breakout + 초고변동 sizing shrink), 39 회귀 테스트
- [x] **#32 Pair Trading** — 완료 (2026-05-10) — pair_trading.py + PairSignal 이동, strategies.py 재export 허브화; 2026-05-18 확장 — `pair_trading_contract.py` ABC 구현 (OLS hedge + z-score 평균회귀, supports_pair=True, leg_bias 설명 context, 페어 *directionally neutral 아님* 한계 명시), 46 회귀 테스트

**Phase 4 (전략) 분리 라인 완료** — #29~#33 모두 ✅. 다음은:
- [x] **#34 Kimp Formula** — 완료 (2026-05-10) — `app/market/kimp.py` float 단일 진리 소스; 2026-05-18 확장 — `app/market/kimp_calculator.py` Decimal 표준 계산 모듈 (KimpInputs/KimpResult/KimpCalculatorConfig + Direction·ConvergenceState·DislocationKind + compute_kimp + calculate_fee_adjusted_premium_bps + build_kimp_context + classify_structural_vs_temporary_dislocation), 43 회귀 테스트, direct_order_allowed 영구 False
- [x] **#35 Kimp Guards** — 완료 (2026-05-10) — kimp_guards.py + strategy 위임 + kimp_anomaly 추가; 2026-05-18 확장 — `app/strategies/kimp_risk_guards.py` 구조적 KimpGuardDecision API (KimpGuardInput/Reason/Decision/Config + 8 가드 함수 + evaluate_kimp_guards + build_kimp_guard_context), 52 회귀 테스트, direct_order_allowed/used_for_order 영구 False
- [x] **#36 Funding Cost Guard** — 완료 (2026-05-10) — funding.py + 2개 가드 + evaluate_entry_guards 8단계 확장; 2026-05-18 확장 — `app/risk/funding.py` 구조적 FundingGuardDecision API (FundingCostGuard 클래스 + FundingCostInput/Estimate/Snapshot/Reason/Decision/Config + evaluate_funding_entry/hold + compute_funding_estimate + build_funding_guard_context), 45 회귀 테스트, direct_order_allowed/used_for_order 영구 False

**Phase 5 (Agent) 시작** — 다음은 개별 Agent 보강:
- [x] **#37 Agent Architecture** — 완료 (2026-05-10) — AgentBase + 4 sub-agent + 파이프라인 + catalog API
- [x] **#38 Risk Officer Agent** — 완료 (2026-05-10) — 7개 가드 + risk_context_from_manager 헬퍼 + e2e 통합
- [x] **#39 Signal Quality Agent** — 완료 (2026-05-10) — QualityBreakdown + QualityReport/news/freshness/kimp 통합
- [x] **#40 Anomaly Agent** — 완료 (2026-05-10) — Quality/Notices/News/Kimp hard veto 통합 + anomaly_context_for 헬퍼
- [x] **#41 Explain Agent** — 완료 (2026-05-10) — explain_signal/decision/pipeline + 3 포맷 + 한국어 액션 라벨
- [x] **#42 Daily Report Agent** — 완료 (2026-05-10) — AuditLog 집계 + OrderSummary/AgentSummary/key_events + markdown/plain 렌더링
- [x] **#43 Theme Insight Agent** — 완료 (2026-05-10) — 심볼별 통합 브리핑 (테마/뉴스/공지/김프) + outlook 분류
- [x] **#44 Loss Tagging Agent** — 완료 (2026-05-10) — 10개 카테고리 분류 + primary/contributing
- [x] **#45 Performance Agent** — 완료 (2026-05-10) — 승률/PnL/PF/MDD + 전략·손실 카테고리 분해
- [x] **#46 Compliance Agent** — 완료 (2026-05-10) — CLAUDE.md 9개 안전 원칙 자동 점검 + Settings 통합

**Phase 5 (Agent) 10/10 ✅ 전체 완료**

**Phase 6 (리스크/주문실행) 13/13 ✅ 전체 완료** (2026-05-10):
- ✅ #47 RiskManager / ✅ #48 Position Limit / ✅ #49 Loss Limit / ✅ #50 Kill Switch
- ✅ #51 Order Guard / ✅ #52 AI Permission Gate / ✅ #53 Order Gateway / ✅ #54 OrderExecutor
- ✅ #55 Manual Approval / ✅ #56 PaperTrader / ✅ #57 Live Shadow
- ✅ #58 AI Assist / ✅ #59 AI Execution Gate

**Phase 7 (백테스트/승격) 7/7 ✅ 전체 완료** (2026-05-10):
- ✅ #60 Backtest Engine / ✅ #61 Metrics / ✅ #62 Walk-forward / ✅ #63 Monte Carlo
- ✅ #64 Promotion Gate / ✅ #65 Paper Gate / ✅ #66 AI Assist Gate

**Phase 9 (웹/PWA/배포) 9/11 ✅** (2026-05-10):
- ✅ #73 Agent-first Dashboard / ✅ #74 Approval UI / ✅ #75 Risk Panel
- ✅ #76 PWA / ✅ #78 Frontend Integration / ✅ #80 Admin Login
- ✅ #83 Auto Update
- ◕ #79 Pages Demo (doc 존재, mock data 후속) / ◕ #81 Tailscale (doc 존재)
- ☐ #77 Notifications (Telegram — secret 규제로 별도 PR)
- ☐ #82 Tauri (데스크탑 빌드 — 별도 PR)
- **Phase 8 (선물)**: 의도적 후순위
- **Phase 9 (UI/PWA)**: #74 Approval UI, #75 Risk Panel, #76 PWA, #77 Notifications, #78 Frontend Integration
- **Phase 10 (운영/테스트)**: #85 Integration Tests, #86 Staging, #87 Audit Log 보강, #88 Backup, #89 Monitoring, #90 MVP Gate, #91 Pre-market, #92 Release Notes, #93 Security Scan
- **Phase 11 (분석 고도화)**: #94 Alpha Decay, #95 Correlation Guard, #96 Loss Tagging (#44에서 부분 구현)
- [ ] **#40 Anomaly Agent 보강** — 추출 완료, 데이터 품질(#17/#18) 통합 후속
- [ ] **#41–#46 기타 Agent** — Explain / Daily Report / Cost Tagging 등
- [ ] **(후속)** UpbitAdapter/OkxAdapter/BinanceAdapter 가 limiter 자동 적용하도록 통합
- [ ] **(후속)** Upbit/OKX/Binance 어댑터에 OHLCV 통합 — volume_24h/regime 정확도 향상
- [ ] **(후속)** ccxt 기반 어댑터 공통 로직 추상화 (BaseCcxtAdapter) — OKX/Binance 중복 제거
- [ ] **(후속)** Strategy/Collector 가 QualityReport·NoticeRegistry 를 직접 소비하도록 통합 — KimpStrategy 의 liquidity_ok/fx_anomaly_ok/deposit_withdrawal_ok 자동 채움 (별도 PR)
- [ ] **(후속)** NoticeRegistry DB 영속화 (현재 메모리)
- [ ] **#73 Agent-first Dashboard** — Header/StatusCard 위에 실제 위젯 (Kill Switch / Approval 큐)
- [ ] **#74 Approval UI** — React로 마이그레이션 (현재 placeholder만)
- [ ] **(후속)** OrderGateway/AgentOrchestrator를 신규 facade(OrderAuditLog/AgentDecisionLog)로 마이그레이션 — 동작 변경 없는 리팩터로 별도 PR
- [ ] **(후속)** AuditLog DB backing store 연동 — 메모리·CSV 외 DB 영속화 (`AuditEvent` 모델 활용)

## 2026-05-15 복구 세션 메모

- `cointrade.zip` 과 워킹트리 일치 확인 (260 파일) — 별도 복원 필요 없음
- `git init` + remote = `https://github.com/1976haru/coin.git` (cointrade 디렉토리 자체를 독립 저장소로)
- `.gitignore` 보강: `node_modules/`, `dist/`, `.pytest_cache/`, `cointrade.zip`, `*.db` 등 추가
- `security_scan.py` 에 `# noqa: security-scan` per-line opt-out 추가 — 가짜 토큰 fixture 2건 (`test_audit_foundation.py`, `test_phase10_scripts.py`) 합법화
- pytest 1302 통과 / 1 skip — 전체 그린
- `security_scan.py` finding 0 / `mvp_gate.py` PASS / `pre_market_checklist.py` warning 1 (ADMIN_TOKEN 기본값)

## 2026-05-17 1~12번 재검증 세션 메모

작업 범위: 체크리스트 1~12번 PASS 여부를 *파일 존재* 가 아닌 *실 테스트 결과* 로 재확인. 13번 이후는 손대지 않음.

### 1~12번 상태표 (실 테스트 기준)
| # | 항목 | 상태 | 검증 근거 |
|---:|---|:---:|---|
| 1  | Product Scope         | ✅ | `tests/test_docs_product_scope.py` 통과 |
| 2  | Strategy Portfolio    | ✅ | `tests/test_docs_strategy_portfolio.py` 통과 |
| 3  | Operating Modes       | ✅ | `tests/test_mode_capabilities.py` + `tests/test_modes_flags.py` 통과 |
| 4  | Safety Principles     | ✅ | `tests/test_docs_safety_principles.py` 통과 |
| 5  | Agent Trader Naming   | ✅ | `tests/test_app_info_and_branding.py` 통과 (브랜드 드리프트 회귀 포함) |
| 6  | Backend Skeleton      | ✅ | `tests/test_health.py` 통과 + 패키지 트리 정합 |
| 7  | Frontend Skeleton     | ✅ | `tests/test_frontend_skeleton.py` 통과 + `npm run typecheck` / `npm run build` / `npm test` 모두 통과 |
| 8  | Shared Schemas        | ✅ | `tests/test_schemas.py` 통과 (is_order_intent 강제 포함) |
| 9  | Config Layer          | ✅ | `tests/test_config_layer.py` + `tests/test_config.py` 통과 (redaction / validate / .env.example 파리티) |
| 10 | Feature Flags         | ✅ | `tests/test_feature_flags.py` 통과 (cross-test 모듈-식별자 오염 수정 후 안정) |
| 11 | Audit Foundation      | ✅ | `tests/test_audit_foundation.py` + `tests/test_audit_events_helpers.py` 통과 |
| 12 | CLAUDE.md             | ✅ | 루트 `CLAUDE.md` 존재 + 안전 원칙 회귀(`tests/test_docs_safety_principles.py`)에서 인용 검증 |

### 이번 세션에서 복구한 결함

1. **ApprovalQueue 타입힌트 잠재 버그** (`backend/app/execution/approval_queue.py`)
   - `def list(self) -> list[dict]:` 가 class body 내에서 `list` 이름을 메서드로 가리고,
     이후 `def pending(self) -> list[ApprovalItem]:` 의 annotation 이 `function not subscriptable`
     TypeError 를 발생 (Python 3.14 lazy annotation 시점). `inspect.signature`/`typing.get_type_hints`
     를 쓰는 외부 도구가 호출되면 즉시 깨짐.
   - 수정: 파일 상단에 `from __future__ import annotations` 추가 — annotation 을
     문자열로 보관하여 평가 시점을 늦춤. 동작 변경 0, 회귀 위험 0.

2. **Frontend typecheck 스크립트 오류** (`frontend/package.json`)
   - `tsc -b --noEmit` 는 TS5094: "Compiler option '--noEmit' may not be used with '--build'"
     오류로 항상 실패. project references 빌드 모드에서 `--noEmit` 은 의미가 모호하기 때문.
   - 수정: `typecheck` 를 `tsc -b` 로 단순화. 루트 `tsconfig.json` 은 이미 `noEmit: true`,
     참조 프로젝트 `tsconfig.node.json` 은 composite (output 보존 필요) 이라 그대로 둠.
   - 결과: `npm run typecheck`/`npm run build`/`npm test` 모두 그린.

3. **FeatureFlags 모듈-식별자 cross-test 오염** (`backend/app/core/feature_flags.py`, `backend/tests/test_modes_flags.py`)
   - `FeatureFlags` dataclass default 가 import 시점에 env 를 평가 (`_bool(...)` 호출)
     하던 구조 → 환경변수 변경을 반영하려면 `importlib.reload` 필요했음.
   - `test_modes_flags.py::_fresh_flags()` 가 `importlib.reload(app.core.feature_flags)` 호출 →
     `FeatureDisabledError` 클래스 객체가 재생성 → 이후 `test_feature_flags.py` 의
     `pytest.raises(FeatureDisabledError, ...)` 가 *과거 클래스* 를 잡으려 해 매칭 실패
     (`test_assert_live_trading_blocked_by_default` 등 3개 실패).
   - 수정:
     - `FeatureFlags` 필드를 `field(default_factory=lambda: _bool(...))` 로 전환 → env 는
       인스턴스 생성 시점에 평가. `importlib.reload` 불요.
     - `_fresh_flags()` 는 단순 `get_feature_flags()` 호출로 단축.
   - 결과: 단독 실행/병행 실행 모두 그린 (32/32).

### 검증 결과

**Backend** (`cd backend && python -m pytest -q`):
- 1404 passed / 1 skipped / 0 failed

**Backend focused (1~12번 범위)**:
```
python -m pytest \
  tests/test_docs_product_scope.py tests/test_docs_strategy_portfolio.py \
  tests/test_mode_capabilities.py tests/test_modes_flags.py \
  tests/test_docs_safety_principles.py tests/test_app_info_and_branding.py \
  tests/test_frontend_skeleton.py tests/test_schemas.py \
  tests/test_config_layer.py tests/test_config.py \
  tests/test_feature_flags.py tests/test_audit_foundation.py \
  tests/test_audit_events_helpers.py tests/test_health.py -q
```
- 349 passed / 1 skipped / 0 failed

**Frontend** (`cd frontend`):
- `npm run typecheck` — exit 0, 출력 없음 (그린)
- `npm run build` — `66 modules transformed`, `built in ~600ms`
- `npm test` — 4 tests passed (App.test.tsx)

### 작업 범위 확인

- 13번 Database Schema 이후 항목은 손대지 않음. 기존 상태 그대로 유지.
- 실거래 LIVE 주문 코드 추가 없음.
- `ENABLE_LIVE_TRADING` / `ENABLE_AI_EXECUTION` / `ENABLE_CRYPTO_FUTURES_LIVE` 기본값 변경 없음 (모두 default False 유지).
- frontend 에 secret/token/api key 추가 없음.
- Upbit/OKX/Binance 실거래 연동 확장 없음.
